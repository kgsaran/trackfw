# Vault de Conhecimento

> Ponto de entrada de conhecimento do projeto para agentes e pessoas.

## Índice

- [regra-aceitava-placeholder-e-apertar-a-regra-esvazia-o-cenario-192-2026-09-26](regra-aceitava-placeholder-e-apertar-a-regra-esvazia-o-cenario-192-2026-09-26.md) — 🔴 **`req_has_roadmap` aceitava `roadmap: none` porque o código usava `extractFrontmatterField` enquanto o comentário afirmava `extractRefPath`** — e o que decidiu o aperto não foi a medição do validador, foi o contrato do GERADOR em `cli-parity.md`, que já tratava `none` como placeholder a preencher (divergência interna, não escolha de política). Medido nos 231 REQs com dois binários: 12 → 13 acusadas, **zero vínculo legítimo perdido**; a população em risco é `frontmatter vazio − já acusadas = 7`, não o total. 🔴 **A armadilha:** migrar a regra de `contentHasMarkerValue` para `extractRefPath` **esvazia** a Direção B do Cenário 192 (a sabotagem da ancoragem deixa de mudar o veredito e `assert_lacks_pattern` reprova sem defeito), e nenhuma composição salva — a prosa passa a ser recusada por duas razões independentes; conserto: Direção B migra para o campo ADR (citando o CAMINHO, senão `adr_orphan` reprova os dois braços) e uma Direção C nova mede a ancoragem do leitor novo. Inclui por que o alvo de vínculo das fixtures tem de morar em `abandoned/` (o `find docs/roadmaps/backlog` dos Cenários 24/25/26 devolve duas linhas) e por que a sabotagem de `EqualFold` só compila como disjunção

- [o-gerador-perdia-a-req-dentro-do-body-e-o-cenario-24-fixa-o-bloco-de-acs-2026-09-26](o-gerador-perdia-a-req-dentro-do-body-e-o-cenario-24-fixa-o-bloco-de-acs-2026-09-26.md) — 🔴 **`roadmap new --from-req` deixava a REQ com `roadmap: ""` por DOIS motivos que só aparecem lendo as duas funções juntas**: (1) `NewRoadmapFromREQ` monta o `req:` DENTRO do `Body` e chama `NewRoadmapFromContent` **sem `REQPath`** — qualquer correção condicionada a esse campo nasce **inerte** no caminho `--from-req`, sem erro nenhum; (2) `syncREQReferences` (o sync que o `roadmap move` já anuncia) **descarta por construção** a REQ recém-criada, porque só reescreve quem **já aponta** para o basename movido — reaproveita-se o ESCRITOR (`rewriteREQRoadmapRefWith`, critério por predicado), nunca a descoberta. Inclui a assimetria deliberada dos dois predicados (frontmatter é máquina, corpo é prosa humana), e 🔴 a armadilha de instrumento: o **Cenário 24** do `check-gates-falsify.sh` fixa o bloco de ACs como literal de 4 linhas e exige **exatamente 2 ocorrências** — preencher o bloco derruba para 1 e o cenário aborta por motivo alheio ao que mede

- [nome-vazio-move-o-primeiro-e-nome-completo-move-o-irmao-maior-2026-09-26](nome-vazio-move-o-primeiro-e-nome-completo-move-o-irmao-maior-2026-09-26.md) — 🔴 **`roadmap move ""` movia o primeiro roadmap do primeiro estado (`Contains(x, "")` é sempre verdadeiro), e — o que ninguém previu — o nome COMPLETO e exato movia o IRMÃO MAIOR** quando o stem é prefixo dele (`...-governance` casava `...-governance-ML-1B.md`): 3 pares assim nos 228 roadmaps e os mesmos 3 nos 231 REQs, medido com binário de HEAD contra binário corrigido sobre cópia do corpus real. `req move` é pior: além de mover, **reescreve o `status:`** dentro do arquivo errado. Por isso recusar ambiguidade **sem** precedência de casamento exato tornaria esses 3 nomes inendereçáveis — trocaria mover-o-errado por não-mover-nenhum. A régua que separa "parcial" (legítimo, 151/228 em fragmentos de 20 chars) de "ambíguo" (defeito, 77/228) é a **cardinalidade dos candidatos**, nunca o comprimento da query. Inclui as duas armadilhas de implementação (duas cópias do laço primeiro-vence — `flat` e `by_agent`, e só `flat` é exercitado pelo corpus real; e por que coleta-de-todos exige filtrar `.md` sob pena de virar recusa) e o sítio de mesma classe deixado para o ML-3A com razão escrita

- [recusa-de-root-em-funcao-void-o-analisador-exige-que-a-recusa-seja-acionada-2026-09-25](recusa-de-root-em-funcao-void-o-analisador-exige-que-a-recusa-seja-acionada-2026-09-25.md) — 🔴 **em função void e não-fatal, `_ = pathguard.RefuseUnverifiableRoot(...)` REPROVA no analisador do ML-7C** (`guard-not-acted-on`), e afrouxar a regra faria uma chamada nua virar guarda que domina a escrita — o sítio passaria tendo apenas **imprimido**. Conserto: extrair o corpo guardado para um irmão que retorna erro e deixar a não-fatalidade numa linha só (`_ = …Entry(…)`). Inclui onde a prova de não-fatalidade fica vazia depois da extração (afirmar no invólucro void, nunca pelo chamador real) e por que o seam para falhar `projectRoot()` vai em `os.Getwd` (`var getwdFn`) e **nunca** em `projectRoot` — o analisador casa provenência pelo identificador literal

- [gatilho-edited-so-existe-no-workflow-e-permissions-de-job-substitui-o-do-workflow-2026-09-25](gatilho-edited-so-existe-no-workflow-e-permissions-de-job-substitui-o-do-workflow-2026-09-25.md) — 🔴 **`on.pull_request.types` e do WORKFLOW: nao existe tipo de evento por job**, entao acrescentar `edited` ao `quality.yml` para atender UM gate dispararia os 13 jobs do arquivo a cada edicao de descricao de PR (medido: 11 sem `if:` + 2 com `if: always()`, nenhum excluindo `edited`) — a fronteira correta e um workflow proprio, nao um `if:` por job que o proximo job novo esquece. Mais dois fatos que custam tempo: **`permissions:` no job SUBSTITUI o do workflow** (declarar so `pull-requests: read` zera `contents` e quebra o `actions/checkout`), e o **payload de um evento `edited` carrega o corpo JA EDITADO** — logo a mensagem *"corpo de ABERTURA"* do gate virava mentira no mesmo commit que ligava o gatilho, e `GITHUB_EVENT_ACTION` nao existe (a acao vive em `.action` no payload). Inclui por que o `GH_TOKEN` do #416 ficou INERTE em CI (`grep -c` = 0) e mascarou a forma 4 ate o #293 mergear verde, e por que **degradacao API→payload nao e vacuidade**: o payload e corpo real, o gate mede; o que se perde e a PRECISAO DA FONTE, e por isso vira aviso anunciado (causa + rc + stderr + `::warning::`), nunca `exit 2`
- [mascarar-code-span-fabrica-indentacao-e-a-mascara-seguinte-engole-a-linha-2026-09-25](mascarar-code-span-fabrica-indentacao-e-a-mascara-seguinte-engole-a-linha-2026-09-25.md) — 🔴 **um passe de mascaramento que troca conteudo por ESPACOS cria sinal de LAYOUT onde nao havia, e o passe seguinte o le como se fosse do autor**: no `check-pr-closing-keyword.sh`, apagar um code span no inicio da linha (`` `trackfw validate` — Fecha #246. ``) fabrica 18 espacos a esquerda, o `INDENT_RE` casa, o bloco abre e a declaracao portuguesa e **engolida** — medido rc=0 pelo caminho real do gate, falso negativo vivo sobre o proprio defeito que o gate guarda. A "divida de contagem" do ML-N1 (304 linhas apagadas contra 80 do censo do parecer) **nao era contabilidade**: decomposta, 80 = 54 dentro de cerca + 26 fora + **0** abrindo bloco, logo o `INDENT` apaga 0 linhas reais e as 304 eram 100% fabricadas. Regra geral: **passe que le POSICAO (indentacao, coluna, inicio de linha) tem de ler o texto ORIGINAL**, mesmo escrevendo no derivado; e divergencia entre dois censos da mesma zona e hipotese de DEFEITO ate alguem decompor os dois numeros. Inclui por que inverter a ordem (INDENT antes de FENCE) troca um defeito por outro menos visivel

- [cenario-de-falsificacao-fixa-linha-literal-do-gate-e-renomea-la-mata-o-chunk-2026-09-25](cenario-de-falsificacao-fixa-linha-literal-do-gate-e-renomea-la-mata-o-chunk-2026-09-25.md) — 🔴 **`check-gates-falsify.sh` sabota gates por `sed` sobre uma LINHA LITERAL do fonte, e isso é um contrato invisível entre dois arquivos**: o cenário `s182` fixa `if num not in english:` no `check-pr-closing-keyword.sh`; reescrevê-la para a forma idiomática `if num in english: continue` fez a sabotagem não casar. O cenário reprova **fail-closed e nomeando a causa** (*"a sabotagem nao alterou nada… parou de medir o que promete"*) — mas sob `set -euo pipefail` o `chunk_2` **morre no meio** e o driver cospe **+10 rótulos "esperado AUSENTE" sem defeito nenhum**, então parece que você quebrou 11 coisas e quebrou **uma**. **Leia o primeiro `FAIL` do chunk, não a lista de ausentes.** Inclui o `grep` que detecta o acoplamento antes de gastar os ~13 min de `make quality`, e por que a correção é restaurar a linha (a sabotagem tem de representar a regressão) e não atualizar o literal do cenário

- [o-epilogo-no-meio-do-arquivo-apaga-o-ultimo-cenario-em-enumerate-e-o-contador-congela-2026-09-24](o-epilogo-no-meio-do-arquivo-apaga-o-ultimo-cenario-em-enumerate-e-o-contador-congela-2026-09-24.md) — 🔴 **o bloco de saída do modo `enumerate` vivia 15 linhas ANTES do último cenário**: no censo de Windows (que roda em enumerate e reprova por construção) o chunk saía antes do Cenário 200 e os 9 rótulos `interp-path/*` sumiam — reportados pelo driver como *"rótulo esperado AUSENTE"*, que lê como chunk morto em vez de *"a execução parou aqui"*. Medido 0 vs 9 rótulos no par A/B de chunks materializados. **Segundo defeito, mesma causa, inédito:** a leitura de `falsify_success_n` também vinha antes do último cenário — o número impresso e comparado com `FALSIFY_SUCCESS_FLOOR` era um retrato tirado cedo demais. Razão estrutural (o último segmento vai do último cabeçalho ao EOF, logo "append no fim do arquivo" cai DEPOIS do epílogo), a guarda fail-closed em 4 direções dentro de `gen-falsify-chunks.py` (o único sítio da gramática `HDR_PAT`), por que `CHUNK_COMPLETE` ausente é CORRETO nesse braço, e a armadilha do `TRACKFW_ROOT_DIR` que faz um A/B inteiro devolver 0/0

- [path-curado-no-windows-tres-medicoes-que-invertem-o-diagnostico-2026-09-24](path-curado-no-windows-tres-medicoes-que-invertem-o-diagnostico-2026-09-24.md) — 🔴 **três medições de Windows que fazem o gate acusar o sujeito errado**: (1) `subprocess.run(env={"PATH":curado})` **não** cura a resolução — o `CreateProcess` usa o PATH do processo **PAI** (medido: `gh 2.100.0` executou sob PATH curado sem `gh`, com `shutil.which` devolvendo "não resolvido"); (2) `git fetch`/`ls-remote` de remoto **local** morre com `0xC0000005` (`3221225477`) quando falta **`sh`** no PATH — 4 células medidas, com controle que elimina "faltava um diretório qualquer", e **não** é DLL do git (seria `0xC0000135`); (3) `ln -s` degrada para **cópia** e mata o gate sob `set -e` ao construir symlink pendurado — `MSYS=winsymlinks:nativestrict ln -s` cria symlink de verdade, e a saída sem symlink é **`FAIL` nomeando a garantia**, nunca `SKIP`

- [heredoc-sem-terminador-engole-o-resto-do-arquivo-e-o-piso-anda-com-a-correcao-2026-09-24](heredoc-sem-terminador-engole-o-resto-do-arquivo-e-o-piso-anda-com-a-correcao-2026-09-24.md) — 🔴 **duas armadilhas de varredor de corpo Python embutido em shell**: (1) heredoc sem terminador **engole o arquivo até o EOF** e remove blocos do censo em silêncio — medido 140→109 corpos numa simulação cujo conversor esqueceu a linha `PY`; é a forma fail-open da família, e só um piso sobre **corpos reconhecidos** a pega; (2) 🔴 **o piso anda junto com a correção**: esta REQ tem dois precedentes vivos (`check-thirdparty-parity.sh:167` mantém o corpo EXPANSÍVEL, `:176` o torna LITERAL), então calibrar pelo estado de hoje faria o gate reprovar quem aplicou o remédio recomendado — calibre pelo **pós-correção mais desfavorável** (medido: 130/70 vs 140/83). Inclui por que dois pisos, e qual deles pega o **classificador de citação quebrado** (65 corpos / 5 expansíveis)

- [a-correcao-que-melhora-o-gate-esvazia-a-fixture-da-guarda-dele-2026-09-24](a-correcao-que-melhora-o-gate-esvazia-a-fixture-da-guarda-dele-2026-09-24.md) — 🔴 **uma guarda cuja fixture é o dado real é falsificável por ACIDENTE**: o ML-2K migrou as 2 alegações da tabela `ALLEGATIONS` para o marcador inline (forma preferida) e a guarda de obsolescência passou a iterar **zero** e reportar verde — a classe de defeito que o gate existe para atacar, reproduzida pela correção que o melhorava. Conserto: **fixture injetável** por arquivo + **três** estados (exercitada · "nada a verificar" · 🔴 "não fui exercitada" quando há isenção concedida sem alegação examinada) + obsolescência também do **marcador inline**, com **uma** regex ancorada para censo e consumo

- [marcador-de-contencao-pode-ser-falso-guarda-de-dir-nao-cobre-a-folha-2026-09-21](marcador-de-contencao-pode-ser-falso-guarda-de-dir-nao-cobre-a-folha-2026-09-21.md) — `RejectSymlinks(root, dir)` nunca desce abaixo de `dir`: guardar o diretório e escrever o arquivo dentro deixa a folha livre (13 de 18 sítios em `scaffold.go`); e o marcador `write-containment-allowed` **afirma sem provar** — houve um falso, com o gate verde por cima
- [analisador-de-contencao-a-populacao-e-a-decisao-cara-nao-o-predicado-2026-09-25](analisador-de-contencao-a-populacao-e-a-decisao-cara-nao-o-predicado-2026-09-25.md) — 🔴 num analisador de guarda-antes-de-escrever, **a população é o ML inteiro, não o predicado**: "todo primitivo de escrita" pede ~130 exceções (que **é** o T3 realizado), e "só o que tem taint de root" alcança **9** sítios e **perde o caso-bandeira do #400** (`"lefthook.yml"` é literal relativo). A regra que fecha: **função que guarda alguma coisa põe todas as suas escritas em população**. Mais: `a, err := f()` (multi-valor) **mata o taint em silêncio** e escondeu `syncREQReferences` inteiro; `Dir` é o pai, `Base` é o último componente, e literal com `/` tem N componentes; a assimetria guarda-no-pai/escrita-no-filho é **inegociável** (é o gap de folha); T5 (fallback do resolvedor) **AST não alcança, ponto**; e o corpus mora em `testdata/*.go.txt` com sha256 porque commit-ish não sobrevive a `prune` nem existe em clone de CI
- [beneath-entre-namespaces-degrada-escopo-da-guarda-em-vez-de-recusar-2026-09-25](beneath-entre-namespaces-degrada-escopo-da-guarda-em-vez-de-recusar-2026-09-25.md) — 🔴 **escape VIVO em `trackfw adr new`**: escreve fora do projeto por um `docs` symlinkado, `rc=0`, mudo, num `cd` normal de login shell. `os.Getwd()` do Go honra `$PWD` e devolve `/tmp/p` enquanto `projectRoot()` resolve `/private/tmp/p` → `Beneath(pr, filepath.Abs(adrDir))` compara **namespaces diferentes**, dá `false`, e o `adr.go:47-50` **não recusa: rebaixa o escopo** de projeto para global, que por desenho nunca inspeciona o ancestral. Regra geral: quando um descasamento de caminho escolhe **qual controle usar**, o fallback tem de ser o **mais estrito** — fallback permissivo troca um falso positivo barulhento por um escape mudo. `req new`/`roadmap new` recusam certo (root e alvo do mesmo `projectRoot()`); só `adr` escapa. Um AST de **alcançabilidade** aprova este sítio (o fluxo passa por `pathguard`) — falta o predicado de **provenância do 1º operando**. 🔴 **Fechado pelo ML-7A** (`adrGuardPaths`): `adrDir` relativo ⇒ escopo de projeto **incondicional**, `Beneath` só sobrevive onde pode **aumentar** a estritura — e a mutação mediu que a correção **óbvia** (`EvalSymlinks` no alvo) é **pior que o defeito**: abre também a arm de `$PWD` resolvido, que hoje recusa

- [carga-de-cpu-vem-da-suite-de-falsificacao-vezes-agentes-paralelos-2026-09-22](carga-de-cpu-vem-da-suite-de-falsificacao-vezes-agentes-paralelos-2026-09-22.md) — a suíte de falsificação roda 8 chunks em paralelo e compila binário por cenário; somada a N agentes com `go test ./...`, satura 10 CPUs. `gitstatusd` e `redirect_listener` são inocentes (medidos). Use `TRACKFW_FALSIFY_JOBS=4` e exija teste do pacote tocado, não de `./...`

- [check-barrier-go-bin-vacuity-trap-2026-09-18](check-barrier-go-bin-vacuity-trap-2026-09-18.md) — `check-barrier.sh` sem `GO_BIN` explícito compila o binário correto do `$ROOT_DIR`; sabotagem em `/tmp` é invisível — sempre passar `GO_BIN=/tmp/sab/trackfw-sabotaged` ao testar binários mutados

- [barrier-cascade-isolation-wave-label-malf-2026-09-18](barrier-cascade-isolation-wave-label-malf-2026-09-18.md) — `ParseWaves` abortava documento inteiro ao encontrar wave malformada (ex: `1b`); 2 roadmaps reais eram invisíveis a `barrier --wave N`; ML-1D revoga ADR-2026-07-29 decisão 16, implementa cascade isolation
- [git-for-each-ref-origin-head-short-e-origin-nao-origin-head-2026-09-17](git-for-each-ref-origin-head-short-e-origin-nao-origin-head-2026-09-17.md) — `%(refname:short)` de `refs/remotes/origin/HEAD` emite `"origin"` (não `"origin/HEAD"`); filtro errado fazia `deriveOriginDefaultBranch` falhar em repos com origin/HEAD + único branch não-main
- [config-defaults-vs-credential-guard-defaults-divergence-2026-09-17](config-defaults-vs-credential-guard-defaults-divergence-2026-09-17.md) — `config.Load().Rules` injeta defaults do pacote (`stale_wip:"warning"` etc.) que `credentialGuardDefaultSeverity` desconhece → falso-positivo de weakening; usar `config.ParseRulesFromContent()` para "o que o usuário escreveu" vs `config.Load()` para "configuração efetiva"
- [origin-main-anchor-pkg-var-stale-state-2026-09-17](origin-main-anchor-pkg-var-stale-state-2026-09-17.md) — `currentOriginMain` (var de pacote do anchor origin/main) fica stale entre testes que chamam `ruleSeverity()` direto; padrão: `t.Cleanup(func() { currentOriginMain = originMainAnchor{} })` + `currentOriginMain = loadOriginMainAnchor()` antes de chamadas diretas
- [gate-lista-proibida-aprova-por-omissao-e-glob-yml-perde-yaml-2026-09-17](gate-lista-proibida-aprova-por-omissao-e-glob-yml-perde-yaml-2026-09-17.md) — gate de lista proibida aprova qualquer mecanismo não listado por omissão; `*.yml` não casa `*.yaml`; fix: lógica invertida (prova positiva de `go build .../cmd/trackfw`) + enumeração dupla

- [jira-base-url-config-env-exfiltration-2026-09-17](jira-base-url-config-env-exfiltration-2026-09-17.md) — config URL + env token = combinação indistinguível de uso legítimo mas permite exfiltração; recusa por combinação, opt-in por env var; redirect cross-hostname seguro (stdlib strippa Authorization), same-host-porta-diferente inseguro (CheckRedirect fecha)
- [chdir-removeall-nao-forca-getwd-falhar-no-windows-2026-09-17](chdir-removeall-nao-forca-getwd-falhar-no-windows-2026-09-17.md) — `os.RemoveAll(CWD)` falha no Windows (SO bloqueia); use `GetwdFn` injetável em vez de truque de filesystem; `EvalSymlinks` em caminho inexistente falha portavelmente em todas as plataformas

- [falsify-success-counter-subshell-misplacement-2026-09-16](falsify-success-counter-subshell-misplacement-2026-09-16.md) — mensagem de sucesso deslocada (linha 6272 de 6641) mentia sobre resultado; contador "183" hardcoded; fix: arquivo `$FALSIFY_SUCCESS_TALLY` + 65 pontos instrumentados; falsificações A (mensagem suprimida) e B (contador -1) provadas
- [falsificar-fonte-python-deixa-pyc-obsoleto-2026-09-13](falsificar-fonte-python-deixa-pyc-obsoleto-2026-09-13.md) — 🔴 mutar fonte Python para falsificar deixa `.pyc` que **sobrevive à restauração**: disco e `git status` limpos, interpretador com o valor antigo; o sintoma aponta para código e enganou duas pessoas seguidas (o agente e o arquiteto) — limpar `__pycache__` faz parte da restauração
- [npm-offline-port1-syn_sent-vacuidade-2026-09-12](npm-offline-port1-syn_sent-vacuidade-2026-09-12.md) — porta 1 causa SYN_SENT (não ECONNREFUSED) no macOS; `--offline` com cache vazio é o probe correto; com `file:` tarballs + `--omit=optional`, `--offline` passa vacuamente — a prova vive em braço separado com ENOTCACHED
- [opcao-d-npm-cache-eacces-restricao-geral-do-npm-2026-09-12](opcao-d-npm-cache-eacces-restricao-geral-do-npm-2026-09-12.md) — AC4 da validação D: npm recusa install quando cache dir é chmod 555 (restrição geral, não defeito da opção D); variante com HOME ro + cache gravável passa; go-to-wheel v0.2 incompatível com cmd/trackfw/ layout
- [sync-req-references-bootstrap-gap-2026-09-12](sync-req-references-bootstrap-gap-2026-09-12.md) — `syncREQReferences` não estabelece backlink REQ→roadmap quando `roadmap: ""` no frontmatter; `roadmap new --from-req` não grava o backlink na REQ; `Roadmap: none` era placeholder aceito por `contentHasMarkerValue` mas rejeitado por `extractRefPath` (ML-1A); fix pertence ao ML-1B (AC7)
- [nul-byte-literal-no-fonte-torna-o-arquivo-invisivel-a-busca-2026-09-12](nul-byte-literal-no-fonte-torna-o-arquivo-invisivel-a-busca-2026-09-12.md) — 🔴 **consolida 3 notas do mesmo fato (29/08, 06/09, 12/09) e é a prova de que nota não basta**: um byte NUL literal em `npm/src/validator/index.js` e `npm/src/integrations/doctor.js` faz o `grep` pular o arquivo em silêncio (`file` diz texto, `grep` diz binário); 4 ocorrências, 2 REQs abertas com evidência falsa; o controle que faltava é trocar o byte literal pela sequência de escape `\0` + gate — não uma quarta nota
- [dois-falsos-vermelhos-ambiente-e-heuristica-2026-09-12](dois-falsos-vermelhos-ambiente-e-heuristica-2026-09-12.md) — dois falsos vermelhos medidos no fechamento da v7.6.0: `check-branch-new-parity` reprova quando a shell tem `FORCE_COLOR` (o Claude Code seta `FORCE_COLOR=3`, então o público que mais roda o gate local é o que mais vê o falso vermelho); e o aviso de branch não-mergeada de `push`/`ship` usa `git branch -r --no-merged`, que **não detecta squash-merge** — a estratégia padrão deste repo — logo é falso por construção
- [python-roadmap-req-inheritance-defect-2026-09-12](python-roadmap-req-inheritance-defect-2026-09-12.md) — Python `roadmap new --req` não herda agente da REQ (AC11 aberto); Go e Node OK; provável inversão de ordem `resolve_write_agent` vs extração do caminho; descoberto em ML-3B-a ao exercitar Go pela primeira vez no smoke
- [assert-help-contract-vacuous-prose-injection-2026-09-12](assert-help-contract-vacuous-prose-injection-2026-09-12.md) — `(^|[[:space:]])palavra` no grep de help inteiro é re-vacuável por prosa: ML-3A injetou "2+ agents" na descrição do commit e o gate passou mesmo sem o comando registrado; fix: extrair região de listagem por runtime (cobra/commander/argparse) + ancorar em `^[[:space:]]+`
- [angle-bracket-title-parity-trap-2026-09-12](angle-bracket-title-parity-trap-2026-09-12.md) — `"<title>"` (com angle brackets) vs `"title"` em ReqNewLine/RoadmapNewLine causa falha silenciosa em parity-rest; gate check-validate-parity.sh só testa o caso flat, então o bug sobrevive em Go enquanto Node/Python usam a forma correta
- [substring-assert-nao-detecta-drift-de-mensagem-de-paridade-2026-09-12](substring-assert-nao-detecta-drift-de-mensagem-de-paridade-2026-09-12.md) — assert de substring passa mesmo com mensagens completamente diferentes; paridade de string requer igualdade exata + diff de binários; mensagem de ambiguidade de agente alinhada nos 3 runtimes (ML-2E)
- [serve-browser-exec-string-para-argv-seguranca-2026-09-11](serve-browser-exec-string-para-argv-seguranca-2026-09-11.md) — `exec(string)` no browser-open silenciosamente executa comandos extras; exit code mente — use arquivo sentinela; residual Windows: `cmd.exe` re-parseia metacaracteres mesmo via argv; contenção é AC4 (isValidHost)
- [fixture-update-harness-fixtures-dependiam-de-side-effect-pre-loop-2026-09-09](fixture-update-harness-fixtures-dependiam-de-side-effect-pre-loop-2026-09-09.md) — ML-1A: 3 fixtures dos cenários 68/69 dependiam do side-effect pre-loop de `update harness` para criar `.trackfw/scripts/`; ao remover o pre-loop, `printf >>` falhava com "No such file or directory" (diretório ausente) — sintoma: 0 FAIL mas vários GUARDA labels ausentes em cascata; corrigido adicionando os targets explícitos na chamada de setup
- [enum-mode-return-1-e-variavel-de-shell-reintroduzem-o-exit-1-2026-09-08](enum-mode-return-1-e-variavel-de-shell-reintroduzem-o-exit-1-2026-09-08.md) — ML-2B: dois defeitos estruturais só visíveis sob sabotagem — `return 1` num helper chamado nu sob `set -e` aborta o call site igual a `exit 1` (correção: `return 0`, a contagem já foi feita antes); contador em VARIÁVEL DE SHELL não sobrevive subshell (`( ... )`/`$( ... )`), a maioria dos ~199 pontos roda dentro de um — correção: contagem em ARQUIVO sob `$WORK`, que atravessa fronteira de subshell; e `gen-falsify-chunks.py` precisa injetar a checagem de fechamento em TODO chunk, não só no que herda a cauda do arquivo-fonte
- [falsify-gate-windows-python-stub-e-go-bin-de-fixture-sem-go-mod-2026-09-07](falsify-gate-windows-python-stub-e-go-bin-de-fixture-sem-go-mod-2026-09-07.md) — ML-1A/ML-1B: `python3` no Windows resolve para o stub da Microsoft Store (rc=49, "Python was not found" mesmo com Python real instalado — critério de rejeição: rc==0 E stdout=="3"); `GO_BIN` ausente faz sub-scripts copiados p/ fixture buildarem com `cd` no `ROOT_DIR` LOCAL da cópia (sem go.mod) — sintoma `go.mod not found` sem relação com a causa (binário ausente); resolvido em ponto único no preâmbulo (`PY_BIN`/`FALSIFY_GO_BIN`) só dentro de `check-gates-falsify.sh` — os ~40 `check-*.sh` copiados para fixture continuam com `python3` bare, medido como próximo obstáculo, não corrigido
- [comentario-de-cenario-sobre-bloco-so-de-funcoes-vira-fronteira-de-corte-falsa-2026-09-07](comentario-de-cenario-sobre-bloco-so-de-funcoes-vira-fronteira-de-corte-falsa-2026-09-07.md) — ML-2D (reentrega): a causa dos 77 rótulos perdidos + 1 FAIL do particionador de falsify **não** era grafia de cabeçalho (hipótese anterior falsificada) — era um bloco só-de-funções (`# Cenario 166 --`) virando fronteira e caindo num chunk diferente dos seus 5 chamadores, ~8000 linhas depois; `command not found` sob `set -euo pipefail` mata o resto da chunk em silêncio; reproduzido isolado com exit=2 exato do relatório original; correção içou blocos "zero asserção + só função" para preâmbulo estendido, guarda de conjunto nova no driver cobre só `assert_*` (limite declarado, não fechado)
- [leitor-unico-fecha-a-classe-mas-2-abort-vivos-fora-da-familia-guard-2026-09-06](leitor-unico-fecha-a-classe-mas-2-abort-vivos-fora-da-familia-guard-2026-09-06.md) — ML-1G: roteados os 20 sítios de `os.ReadFile`/`fs.readFileSync`/`open(` fora da família guard nos 3 runtimes (7 eram sítios de REGRAS reais com silêncio); achados por MEDIÇÃO, não pela contagem do ML-1C: `validateCredentialGuardModeDowngrade` (Go abortava, Python confirmava downgrade falso sobre QUALQUER OSError) e `integrations.LoadManifest` (fora de `internal/validator`, nunca contado, mas alcançado por `thirdparty_artifact_has_provenance` e abortava nos 3 runtimes) — gate novo `scripts/check-raw-read-ban.sh` com allowlist inline e guarda de vacuidade agregada; declarado não corrigido: `internal/integrations/manifest.go` ainda trava em FIFO (hang fechado, abort fechado, package boundary não)
- [check-validate-parity-6-blocos-fechados-5-com-cenario-1-sem-lever-independente-2026-09-06](check-validate-parity-6-blocos-fechados-5-com-cenario-1-sem-lever-independente-2026-09-06.md) — ML-1F: dos 6 blocos sem cenário (GVP, GVMT, `gbg-claude-relativo`, `gbg-cursor-relativo-present`, 3 fixtures reaproveitados de invalid-json/unreadable/utf16), 3 cenários novos (189/190/191) fecham GVP/GVMT/`gbg-claude-relativo` — os 3 fixtures reaproveitados fecham transitivamente via 186-188; `gbg-cursor-relativo-present` fica sem cenário próprio, medido ao vivo: o booleano `requiresVarOrShellPrefix` do Cursor é compartilhado entre credential_guard e git_branch_guard, e sabotá-lo sempre reprova o bloco 8 PRIMEIRO (mascarando o bloco 9) — não é omissão, é restrição estrutural do código
- [check-validate-parity-9-blocos-4-sem-cenario-de-falsificacao-2026-09-06](check-validate-parity-9-blocos-4-sem-cenario-de-falsificacao-2026-09-06.md) — ML-1E: `check-validate-parity.sh` tem 9 blocos cross-CLI; fechados os 3 do ML-1C (Cenários 186-188), mas GVP (mensagem `git_branch_guard_script_integrity` GLOBAL), GVMT (mensagem `git_branch_guard_hook_resolvable` GLOBAL "missing type"), os 2 fixtures próprios de `git_branch_guard_hook_resolvable` cross-CLI e os 3 fixtures JSON-inválido/ilegível/UTF-16 (ML-1A/1B) continuam sem NENHUM cenário provando que o gate os pegaria se regredissem — enumerado, não corrigido
- [credential-guard-marker-const-compartilhada-entre-3-consumidores-quebra-cenario-80-2026-09-06](credential-guard-marker-const-compartilhada-entre-3-consumidores-quebra-cenario-80-2026-09-06.md) — ML-1D: `credentialGuardScriptMarker`/`gitBranchGuardScriptMarker` alimentam 3 call sites (hook_resolvable projeto/global + script_integrity global), não 1; sabotar a CONST no Cenário 80 quebrava o bloco GLOBAL de `credential_guard_script_integrity` (ML-1C, posicionado antes no `check-validate-parity.sh`) primeiro, mascarando o diagnóstico que o cenário afirma provar — retarget para sabotar só o call site de PROJETO, sem afrouxar o assert nem mudar o comportamento fail-fast do gate
- [item-4-do-harness-precisa-provar-identidade-do-bash-antes-de-invocar-2026-09-05](item-4-do-harness-precisa-provar-identidade-do-bash-antes-de-invocar-2026-09-05.md) — o retarget do ML-2C (item 4, `check-parity-contract-coverage.sh` via `bash`) mediu INCONCLUSIVE no primeiro run Windows real (33986718256) porque `bash` por nome cru resolveu no stub do WSL, não no Git Bash — mesma causa raiz do grupo B; correção em `scripts/windows-repro/resolve-bash.ps1` (`Resolve-ProvenBash`), prova identidade (`--version` contém `GNU bash`) antes de invocar, e acusa por nome quando nenhum candidato prova — retarget funcionou, é resultado bom, não regressão
- [nem-todo-skip-de-execbit-precisa-do-mesmo-probe-um-era-copia-colada-2026-09-05](nem-todo-skip-de-execbit-precisa-do-mesmo-probe-um-era-copia-colada-2026-09-05.md) — ML-1A/#279: dos 4 `t.Skip("execute bit not applicable...")` em `scaffold_doctor_test.go`, só 3 mediam o bit de execução de verdade (viram `execBitRepresentavelPara`); o 4º testava precedência de conteúdo sobre modo, e `bytes.Equal` roda **antes** de qualquer checagem de bit em produção — o skip era cópia-colada morta, confirmado por mutação; e distinção geral: quando a garantia sob teste é a PRÓPRIA primitiva privilegiada (symlink, chmod 0500) não há "resto do teste" independente — mantém `t.Skip` condition-based, não vira probe+Stderr (SKIP explícito > PASS vazio)
- [go-isnotexist-e-errors-is-fs-errnotexist-sao-o-mesmo-predicado-para-pathError-de-um-nivel-2026-09-05](go-isnotexist-e-errors-is-fs-errnotexist-sao-o-mesmo-predicado-para-pathError-de-um-nivel-2026-09-05.md) — ML-1C/#276: lendo o GOROOT, `os.IsNotExist` e `errors.Is(err, fs.ErrNotExist)` chamam o **mesmo** `syscall.Errno.Is()` para `*PathError` de um nível (o caso de `os.ReadDir`/`Open`/`Stat`/`ReadFile` direto) — não podem divergir, em nenhum SO; no Windows `ENOTDIR` **é** `ERROR_PATH_NOT_FOUND`, o mesmo código de "caminho ausente", então nem a troca proposta nem a alternativa da própria issue (`errors.Is(err, syscall.ENOENT)`) separam os dois casos ali; a troca é modernização de idioma, não fecha o #276
- [dedup-guard-path-cego-a-backslash-no-windows-2026-09-05](dedup-guard-path-cego-a-backslash-no-windows-2026-09-05.md) — G12/ML-7A: `normalizeGuardPath` (3 runtimes, byte-a-byte) colapsa `//`→`/` mas nunca canoniza `\`↔`/`; no Windows `filepath.Join`/`path.win32.join`/`ntpath.join` sempre emitem separador nativo, então um comando hand-edited com `/` nunca compara igual ao comparando computado — medido **executando de verdade** `path.win32.join`/`ntpath.join` (cross-platform, sem precisar de Windows) e lendo o fonte Go instalado; passa em POSIX porque lá `Join` já usa `/`; produto, não teste — a barra dupla do nome do teste não é a causa, é só o sintoma escolhido para expô-la
- [fixture-de-proveniencia-monta-a-chave-nativa-e-reprova-o-produto-certo-2026-09-04](fixture-de-proveniencia-monta-a-chave-nativa-e-reprova-o-produto-certo-2026-09-04.md) — 🔴 **premissa invertida do ADR-2026-09-04**: as **3** fixtures de `thirdparty-provenance` montam a chave com `filepath.Rel`/`path.relative`/`os.path.relpath` (separador **nativo**), enquanto a produção grava por concatenação explícita com `/` (`ResolveThirdPartySkillDestination`, `render.go:821`) — em Windows **Go e Python reprovam o produto CERTO** e o **Node passa por acidente** (fixture e produto ambos errados, casando); corrigir só o produto do Node viraria o Node de verde para vermelho. Regra geral: quando gerador e verificador de um par são **ambos** de teste, "o teste passa" não discrimina "os dois certos" de "os dois errados" — o discriminante é o terceiro elemento, a produção
- [cwd-preso-e-margem-de-microssegundo-duas-causas-de-windows-invisiveis-em-posix-2026-09-04](cwd-preso-e-margem-de-microssegundo-duas-causas-de-windows-invisiveis-em-posix-2026-09-04.md) — **duas causas de Windows invisíveis em POSIX por construção**: (1) `WinError 32` na **limpeza** do `TemporaryDirectory` é o **cwd do próprio processo** segurando o handle — `tearDown` não resolve (roda depois) e a **ordem do `with`** carrega o peso (`_chdir` por último); o discriminante está na população (os 4 testes com `chdir` falham, o 1 sem `chdir` passa); (2) `stale_wip` "9 days" em vez de "10" **não é fuso horário**, é um `int()` (floor) sobre uma margem **medida de 9 a 21 µs** entre dois relógios — intermitente entre dois runs consecutivos; e um achado de **produto não corrigido**: `stale_wip` fica **muda** no Windows quando `wip/` não é diretório
- [system32-bash-exe-e-o-stub-do-wsl-e-vence-a-resolucao-por-nome-nu-2026-09-04](system32-bash-exe-e-o-stub-do-wsl-e-vence-a-resolucao-por-nome-nu-2026-09-04.md) — **CAUSA RAIZ de 50 testes**: `bash` por nome nu no Windows é ambíguo — a causa é o `lpApplicationName = NULL`, e o stub do WSL é **só um dos três desfechos** (os outros: `WinError 2`, ou funcionar por acaso). Falsificado em Windows real pelo reporter; `env=` **não** muda a resolução
- [exit-1-com-stderr-vazio-e-assinatura-de-processo-que-nao-e-o-script-2026-09-04](exit-1-com-stderr-vazio-e-assinatura-de-processo-que-nao-e-o-script-2026-09-04.md) — `rc=1` com **`stderr` vazio** não é CRLF (rc 2), nem script ausente (rc 127), nem bit de execução, nem `$HOME` (todos rc 0): é a assinatura de **um processo que não é o script**, falando por um `stdout` que os helpers descartam; no run `33810452454` são **50** testes Python (não ~56 — 3 dos 52 são de outros grupos e 2 aparecem só como `SUBFAILED`), enquanto **Node e Go executam os mesmos scripts no mesmo job**; a assimetria sobrevivente é a resolução de `bash` (`LookPath`/libuv com caminho absoluto × `CreateProcess` com `lpApplicationName=NULL`) — hipótese ainda **não medida** no Windows
- [exited-with-null-e-enoent-e-a-fixture-substitui-o-path-nao-o-job-2026-09-03](exited-with-null-e-enoent-e-a-fixture-substitui-o-path-nao-o-job-2026-09-03.md) — `exited with null` é ENOENT do `spawnSync`, e quem descarta o `$PATH` é a **fixture**, não o job; mesma classe do binário sem `.exe`, órfã em Node e Python por erro de atribuição de ML
- [eol-nos-goldens-nao-cura-o-teste-de-golden-porque-o-asset-carrega-o-crlf-2026-09-03](eol-nos-goldens-nao-cura-o-teste-de-golden-porque-o-asset-carrega-o-crlf-2026-09-03.md) — sob `core.autocrlf=true` o CRLF entra pelos **dois lados** da comparação de golden (o asset embedado alimenta o `Render`), então `eol=lf` nos goldens cura **zero** desses testes — o grupo de render é metade de **produto** (parser de frontmatter cego a CRLF), não fixture; a metade que cura de fato (18→14) são comparações artefato-versionado × literal compilado; e a raiz do `.gitattributes` **não pode** receber a regra porque 3 testes exigem que ela seja byte-idêntica ao `GITATTRIBUTES_BLOCK` de produto — inclusive a regra `eol=lf` de que ela própria precisa
- [ciclo-fechado-por-artefato-uma-sabotagem-nao-falsifica-tres-fronteiras-2026-09-03](ciclo-fechado-por-artefato-uma-sabotagem-nao-falsifica-tres-fronteiras-2026-09-03.md) — ciclo fechado `gerador → verificador` dos 3 artefatos: **uma sabotagem só falsifica um terço do gate** (quebrar o resolvedor de REQ deixa `adr_orphan` MAIS forte e `note_orphan` intacto); o status do ADR **não** é falsificável trocando a chave `status:` do frontmatter porque o verificador cai no cabeçalho `> Date: … | Status:` — o discriminante é o **vocabulário**; no Python o ponto de decisão é o default do **argparse** (e ele valida o default contra `choices`); `adr_orphan`/`note_orphan` são `warning` e não movem o exit code; `validate --json` do Go emite uma cauda depois do JSON; e `init` do Python semeia um ADR a mais, o que proíbe métrica por contagem
- [resolvedor-de-req-era-if-else-e-a-uniao-colide-com-o-namespace-vindo-do-disco-2026-09-03](resolvedor-de-req-era-if-else-e-a-uniao-colide-com-o-namespace-vindo-do-disco-2026-09-03.md) — o resolvedor que as **regras** usam era `if/else`, não união: **4 dos 6 layouts** de `req_dir` eram vácuos (não 1), e `traceid` recebia **diretório** em vez da lista resolvida, sobrevivendo ao conserto; ao criar a união, `<agente>/*.md` **colide** com `<estado>/*.md` porque `agents:` é unido ao disco — sem dedup por caminho normalizado toda REQ por-estado conta em dobro; e a métrica "a regra disparou" dá falso verde porque `ref_targets_exist`/`traceid` disparam pelo lado do roadmap
- [dedup-lexical-nao-ve-caixa-diferente-em-apfs-e-o-conserto-obvio-suprime-em-linux-2026-09-03](dedup-lexical-nao-ve-caixa-diferente-em-apfs-e-o-conserto-obvio-suprime-em-linux-2026-09-03.md) — a dedup lexical do resolvedor de REQ **não** vê `req_dir/Backlog` ≡ `backlog` em APFS/NTFS e cada REQ conta em **dobro** (4 violações onde a AC prevê 2, nos 3 CLIs; verde no CI Linux, vermelho na máquina do dev); 🔴 os dois consertos óbvios trocam dupla contagem por **supressão** — lowercase colapsa dois diretórios reais em FS case-sensitive, e identidade de inode não tem chave hasheável portátil no Go nem contrato medido em NTFS; o mecanismo certo é **filtro de existência verbatim** (a grafia do disco é a autoridade, fallback é join cego e nunca lista vazia), e o controle exige um volume case-sensitive real (`hdiutil create -fs "Case-sensitive APFS"`)
- [palavra-chave-de-fechamento-do-github-e-so-em-ingles-e-resolve-e-falso-cognato-2026-09-02](palavra-chave-de-fechamento-do-github-e-so-em-ingles-e-resolve-e-falso-cognato-2026-09-02.md) — o GitHub só fecha issue com `close/fix/resolve` em **inglês**: dos 241 PRs mergeados deste repo, apenas **4** fecharam issue (todos com `Fixes #N`), e `Fecha #246.` do PR #247 não fechou nada; três armadilhas ao gatear isso — `Resolve #N` é falso cognato que **funciona**, a isenção tem de ser **por número de issue** (`Fecha #246` + `Fixes #999` reprova), e a **adjacência** é o que separa 3 linhas reprovadas de 43 no mesmo corpus
- [promise-flutuante-em-action-do-cli-node-e-invisivel-na-fronteira-2026-09-02](promise-flutuante-em-action-do-cli-node-e-invisivel-na-fronteira-2026-09-02.md) — `trackfw context` do CLI Node nunca funcionou (`validate` async desestruturado sem `await`); o defeito mora na **fronteira** comando↔validator e teste com mock não o vê; e **propagar a Promise da `.action()` é infalsificável pelo binário** — `installGlobalHandlers()` converte a rejeição flutuante no mesmo exit 1, em qualquer comando do CLI Node
- [merge-union-preserva-linhas-mas-nao-ordem-e-metrics-depende-de-posicao-2026-09-02](merge-union-preserva-linhas-mas-nao-ordem-e-metrics-depende-de-posicao-2026-09-02.md) — `merge=union` no `.trackfw-log` nunca conflita e **não** perde nem duplica linha (medido, inclusive em adição idêntica e sobreposição parcial), mas concatena `ours` antes de `theirs` e quebra a ordem cronológica; dos leitores do log só o cycle time/WIP age de `internal/metrics/metrics.go` depende de posição — e a resolução manual que ele substitui já duplicou uma linha neste repositório
- [gate-em-cp1252-tem-duas-falhas-distintas-crash-de-print-e-mismatch-por-transcodificacao-2026-09-02](gate-em-cp1252-tem-duas-falhas-distintas-crash-de-print-e-mismatch-por-transcodificacao-2026-09-02.md) — gate sob cp1252 falha de duas formas: crash de `print()` (`→` indefinido) e **mismatch sem crash** (`—` transcodifica para `0x97` e quebra a comparação byte-a-byte python→bash); `PYTHONUTF8`/`-X utf8` não move o stdio e a superfície do `open()` não é simulável no macOS (PEP 540 sob `LC_ALL=C`)
- [cp1252-roundtrip-mascara-o-defeito-o-discriminante-e-decode-de-stdin-2026-09-02](cp1252-roundtrip-mascara-o-defeito-o-discriminante-e-decode-de-stdin-2026-09-02.md) — cp1252 faz round-trip byte-a-byte de UTF-8, então `✓` passa igual antes e depois do fix; o discriminante é o **decode do stdin** com byte indefinido em cp1252 (`0x81` de `Á`), não o encode do stdout — e o residual declarado no ML era o inverso do medido
- [handler-de-erro-em-pythonioencoding-reintroduz-byte-invalido-e-os-3-serve-divergem-2026-09-02](handler-de-erro-em-pythonioencoding-reintroduz-byte-invalido-e-os-3-serve-divergem-2026-09-02.md) — `PYTHONIOENCODING=utf-8:surrogatepass|surrogateescape` (**aceitos** por `check-output-encoding-declared.sh`) escrevem byte inválido no `.trackfw-attention.json`; ao lê-lo, Go e Node devolvem `active:true` com U+FFFD e o **Python devolve `active:false`** (`UnicodeDecodeError` ⊂ `ValueError`) — o banner some só num dos 3 runtimes, sem crash nem log
- [comentario-inline-com-heredoc-derruba-arquivo-da-populacao-do-gate-2026-09-02](comentario-inline-com-heredoc-derruba-arquivo-da-populacao-do-gate-2026-09-02.md) — comentário **inline** citando `<<` arma o rastreador de heredoc e faz o parser descartar o resto do arquivo: em `check-output-encoding-declared.sh` isso tirava o arquivo da **população** e o gate devolvia `exit 0` sobre a regressão que ele existe para pegar (fail-open, guardas de vacuidade cegas); a regra é separar o predicado de **população** (loose) do de **declaração** (strict) — e os dois residuais que sobram são fechados
- [gate-literal-regex-syntax-equivalent-bypass-2026-09-01](gate-literal-regex-syntax-equivalent-bypass-2026-09-01.md) — `check-shell-posix-portability.sh` casa exit 0 (OK) sobre uma regressão real e funcional a `shell:true`/`shell=True` via sintaxe equivalente (`{["shell"]: true}`, `**{"shell": True}`); comentário não é o furo, é a metade positiva (assert_count não exclui comentário) + metade negativa (regex literal evadível) compondo — generaliza a qualquer gate `assert_count`+`assert_no_code_match`
- [makedirs-mode-ignored-on-existing-and-intermediate-parents-2026-09-01](makedirs-mode-ignored-on-existing-and-intermediate-parents-2026-09-01.md) — `mode=` de `os.makedirs`/`Path.mkdir` é ignorado se o diretório já existe e não se propaga a pais intermediários criados no mesmo `parents=True`; `.trackfw` fica em `0o755`, não `0o700`, comprovado ao vivo — pré-requisito de TOCTOU não garantido pelo código
- [fat32-no-macos-finge-0755-e-nao-serve-de-proxy-para-ntfs-2026-09-04](fat32-no-macos-finge-0755-e-nao-serve-de-proxy-para-ntfs-2026-09-04.md) — o VFS `msdos` do macOS **sintetiza** modo POSIX e devolve `0755` para todo arquivo: um volume FAT32 do `hdiutil` **não** reproduz o NTFS (onde `mode&0111` é sempre 0) e faz a sonda do bit de execução responder `True`; `hdiutil` resolve **caixa**, não **modo** — o ramo de NTFS só o CI de Windows exercita, e o que sobra localmente é forçar a sonda a `false` para provar que a guarda não deixa o teste vacuoso
- [goos-guard-e-do-binario-nao-do-host-wsl-continua-protegido-2026-09-01](goos-guard-e-do-binario-nao-do-host-wsl-continua-protegido-2026-09-01.md) — `CurrentGOOS`/`_platform` refletem o SO do binário, não do host; WSL (kernel Linux) reporta `"linux"` e o guard do bit de execução continua ativo lá; suprimir a checagem só no Windows nativo não enfraquece o controle, porque o bit nunca foi discriminante em NTFS
- [falsify-cenario-pina-linha-de-fonte-por-sed-guard-de-plataforma-quebra-2026-08-31](falsify-cenario-pina-linha-de-fonte-por-sed-guard-de-plataforma-quebra-2026-08-31.md) — Cenário 81 de `check-gates-falsify.sh` mira a cláusula `case` inteira via sed; prefixar a condição com `CurrentGOOS != "windows" &&` quebra o casamento textual; remédio é mirar o substring, mesmo precedente do Cenário 179; **3ª ocorrência (2026-09-03, Cenário 183)**: quando o refactor troca a CHAMADA inteira por um helper novo, encurtar a âncora NÃO ajuda (o substring interno aparece 2× e `corrupt_literal` exige 1) — o remédio é **referência forward na fonte**, e 🔴 esse pin não pode reproduzir a âncora verbatim (a contagem é do arquivo inteiro e vai a 2)
- [resolve-symlinks-primitivas-divergem-nos-3-runtimes-folha-inexistente-2026-08-31](resolve-symlinks-primitivas-divergem-nos-3-runtimes-folha-inexistente-2026-08-31.md) — `path.resolve()` do Node não segue symlink (guard no-op); `EvalSymlinks`/`realpathSync` erram sobre folha inexistente (resolver só o diretório-pai); comparar destino resolvido contra `root` não resolvido produz falso positivo (`/tmp`→`/private/tmp` no macOS)
- [lstat-nao-ve-junction-e-guarda-de-folha-nao-olha-ancestral-2026-08-31](lstat-nao-ve-junction-e-guarda-de-folha-nao-olha-ancestral-2026-08-31.md) — `Lstat` classifica junction como `ModeIrregular`, não `ModeSymlink`, e `mklink /J` não exige privilégio; o freio acidental existe só no Go; guarda de folha nunca olha ancestral, em todo SO
- [powershell-modo-argumento-nao-interpola-nem-divide-2026-08-31](powershell-modo-argumento-nao-interpola-nem-divide-2026-08-31.md) — em `shell: pwsh` a vírgula só constrói array em modo expressão; em modo argumento o token é único e a variável **não** é interpolada — o remédio é citar, não escapar
- [if-sem-funcao-de-status-tem-success-implicito-2026-08-30](if-sem-funcao-de-status-tem-success-implicito-2026-08-30.md) — passo pulado com condição verdadeira: o Actions prepende `success()` a todo `if:` sem função de status
- [job-de-windows-largo-so-reproduz-2-dos-11-defeitos-2026-08-30](job-de-windows-largo-so-reproduz-2-dos-11-defeitos-2026-08-30.md) — rodar as 3 suítes completas em Windows não basta: mock/monkeypatch, oráculo ausente ou caminho de produção não atingido escondem 8 dos 11 defeitos da issue #216; e a isolação de `$HOME` dos próprios testes é vácua no Windows nos 3 runtimes
- [uniao-disco-agents-mascara-gate-por-presenca-2026-08-29](uniao-disco-agents-mascara-gate-por-presenca-2026-08-29.md) — a união agents:+disco (REQ-2026-08-29) desarma gates que discriminavam por presença/ausência de namespace; só ordem (declarado-primeiro vs. alfabético) sobrevive como técnica de retarget
- [gates-da-wave-sao-um-comando-por-linha-2026-08-29](gates-da-wave-sao-um-comando-por-linha-2026-08-29.md) — bloco de gate não é script: cada linha é um comando isolado; `grep && {}` reprova quando o grep NÃO acha
- [ambiente-do-dev-e-mais-rico-que-o-do-ci-2026-08-29](ambiente-do-dev-e-mais-rico-que-o-do-ci-2026-08-29.md) — PATH, história do git e locale: três defeitos no mesmo dia, verde local e vermelho no CI
- [teste-disco-em-binario-gitignored-falha-no-ci-2026-09-12](teste-disco-em-binario-gitignored-falha-no-ci-2026-09-12.md) — `assert.ok(fs.existsSync(...))` num binário compilado (gitignored por design) falha sempre no CI; correto é `t.skip()` nomeado quando ausente, assert quando presente
- [paridade-cross-runtime-dentro-do-go-test-quebra-o-job-go-2026-08-29](paridade-cross-runtime-dentro-do-go-test-quebra-o-job-go-2026-08-29.md) — teste cross-CLI no job de um runtime só; e o gate que parecia cobrir chamava só o Go
- [barrier-crlf-divergencia-node-regex-2026-08-29](barrier-crlf-divergencia-node-regex-2026-08-29.md) — CRLF: `.` do JS exclui `\r` e o do RE2 inclui; roadmap do Windows fica ilegível no CLI Node
- [barrier-trust-check-fail-open-em-tmpdir-simbolico-2026-08-29](barrier-trust-check-fail-open-em-tmpdir-simbolico-2026-08-29.md) — NÃO CORRIGIDO: `roadmapTrustForGates` cai em fail-open (executa gates de verdade) quando o sandbox vive sob `$TMPDIR` com componente simbólico (macOS)
- [update-segue-symlink-e-escreve-fora-do-projeto-2026-08-28](update-segue-symlink-e-escreve-fora-do-projeto-2026-08-28.md) — `update`/`discover` seguiam symlink em `.github/workflows/`; corrigido com `lstat` + recusa
- [barrier-so-casa-cabecalho-de-aceite-em-portugues-2026-08-28](barrier-so-casa-cabecalho-de-aceite-em-portugues-2026-08-28.md) — **CORRIGIDO** no PR #217: `barrier` aceita as duas formas de cabeçalho
- [barrier-fence-closing-trailing-content-bypass-2026-08-29](barrier-fence-closing-trailing-content-bypass-2026-08-29.md) — **CORRIGIDO** no PR #217 (ML-3D): fechamento exige só os caracteres da cerca
- [roadmap-title-newline-forges-wave-section-barrier-executes-gate-2026-08-23](roadmap-title-newline-forges-wave-section-barrier-executes-gate-2026-08-23.md) — **VETOR EXECUTÁVEL CORRIGIDO** (verificado 2026-09-02): guarda `\n`/`\r` nos **dois** pontos de entrada dos 3 CLIs (`roadmap.go:132,220` · `roadmap.js:464,547` · `roadmap.py`). Residual sem caminho de execução: `NewREQ`/`NewNote` interpolam título sem guarda, e o `barrier` não tem defesa em profundidade
- [rewrite-frontmatter-newline-injection-escape-hatch-2026-08-21](rewrite-frontmatter-newline-injection-escape-hatch-2026-08-21.md)
- [nil-map-em-projectconfig-campo-novo-atinge-construcoes-antigas-2026-08-21](nil-map-em-projectconfig-campo-novo-atinge-construcoes-antigas-2026-08-21.md)
- [validate-branch-has-wip-roadmap-done-python-rule-null-2026-08-20](validate-branch-has-wip-roadmap-done-python-rule-null-2026-08-20.md)
- [windsurf-amazonq-sem-harness-scope-nunca-cobriveis-por-check-harness-hooks-parity-2026-08-20](windsurf-amazonq-sem-harness-scope-nunca-cobriveis-por-check-harness-hooks-parity-2026-08-20.md)
- [ci-inherited-trackfw-disable-external-commands-vacuous-parity-gate-2026-08-19](ci-inherited-trackfw-disable-external-commands-vacuous-parity-gate-2026-08-19.md)
- [bash-grep-F-embedded-newline-vacuous-match-2026-08-16](bash-grep-F-embedded-newline-vacuous-match-2026-08-16.md)
- [commander-missing-subcommand-help-error-exit-1-2026-08-16](commander-missing-subcommand-help-error-exit-1-2026-08-16.md)
- [ship-checkgovernance-error-stream-wording-divergence-2026-08-16](ship-checkgovernance-error-stream-wording-divergence-2026-08-16.md)
- [found-and-false-suprime-branch-sem-quebrar-go-build-2026-08-16](found-and-false-suprime-branch-sem-quebrar-go-build-2026-08-16.md)
- [cobra-silenceerrors-suppresses-usage-independently-of-per-command-flags-2026-08-16](cobra-silenceerrors-suppresses-usage-independently-of-per-command-flags-2026-08-16.md)
- [python-import-submodule-as-shadowed-by-package-reexport-2026-08-15](python-import-submodule-as-shadowed-by-package-reexport-2026-08-15.md)
- [node-https-redirect-checkredirect-off-by-one-2026-08-15](node-https-redirect-checkredirect-off-by-one-2026-08-15.md)
- [go-regexp-re2-sem-backreference-fenced-block-removal-2026-08-15](go-regexp-re2-sem-backreference-fenced-block-removal-2026-08-15.md)
- [credential-guard-hook-resolvable-nao-detecta-script-ausente-2026-08-15](credential-guard-hook-resolvable-nao-detecta-script-ausente-2026-08-15.md)
- [git-branch-guard-quote-aware-segmentation-2026-08-16](git-branch-guard-quote-aware-segmentation-2026-08-16.md)
- [git-branch-guard-falso-positivo-em-linha-de-mensagem-de-commit-2026-08-16](git-branch-guard-falso-positivo-em-linha-de-mensagem-de-commit-2026-08-16.md)
- [git-branch-guard-pipe-into-while-loses-return-status-2026-08-14](git-branch-guard-pipe-into-while-loses-return-status-2026-08-14.md)
- [git-branch-guard-self-blocking-quote-unaware-splitter-2026-08-14](git-branch-guard-self-blocking-quote-unaware-splitter-2026-08-14.md)
- [python-stdout-buffering-reorders-before-inherited-stdio-subprocess-2026-08-14](python-stdout-buffering-reorders-before-inherited-stdio-subprocess-2026-08-14.md)
- [asset-parity-gate-nao-cobre-o-caminho-de-geracao-mas-o-caminho-e-fiel-2026-08-13](asset-parity-gate-nao-cobre-o-caminho-de-geracao-mas-o-caminho-e-fiel-2026-08-13.md)
- [validador-git-env-bypass-filtre-por-prefixo-2026-08-12](validador-git-env-bypass-filtre-por-prefixo-2026-08-12.md)
- [scenario-50-non-vacuity-obsoleta-pelo-anchoring-no-head-2026-08-12](scenario-50-non-vacuity-obsoleta-pelo-anchoring-no-head-2026-08-12.md)
- [armadilhas-ao-escrever-cenario-em-check-gates-falsify-2026-08-12](armadilhas-ao-escrever-cenario-em-check-gates-falsify-2026-08-12.md)
- [vies-do-tmp-ao-medir-sandbox-de-agente-2026-08-12](vies-do-tmp-ao-medir-sandbox-de-agente-2026-08-12.md)
- [hooks-de-agente-falham-abertos-quando-o-script-nao-resolve-2026-08-12](hooks-de-agente-falham-abertos-quando-o-script-nao-resolve-2026-08-12.md)
- [codex-hooks-de-projeto-so-rodam-em-projeto-trusted-2026-08-11](codex-hooks-de-projeto-so-rodam-em-projeto-trusted-2026-08-11.md)
- [check-agent-hooks-parity-unisolated-home-false-failure-2026-08-08](check-agent-hooks-parity-unisolated-home-false-failure-2026-08-08.md)
- [node-global-credential-guard-dedup-breaks-inject-tests-on-real-home-2026-08-08](node-global-credential-guard-dedup-breaks-inject-tests-on-real-home-2026-08-08.md)
- [credential-guard-parity-test-extractor-rejects-string-concatenation-2026-08-08](credential-guard-parity-test-extractor-rejects-string-concatenation-2026-08-08.md)
- [credential-guard-second-layer-cmd-extraction-json-not-raw-token-2026-08-08](credential-guard-second-layer-cmd-extraction-json-not-raw-token-2026-08-08.md)
- [opencode-tools-key-hard-fail-carregamento-inteiro-2026-08-05](opencode-tools-key-hard-fail-carregamento-inteiro-2026-08-05.md)
- [listas-de-targets-hardcoded-divergem-do-catalog-json-2026-08-05](listas-de-targets-hardcoded-divergem-do-catalog-json-2026-08-05.md)
- [branch-new-exit-code-leak-vs-propagation-2026-08-04](branch-new-exit-code-leak-vs-propagation-2026-08-04.md)
- [check-identity-parity-json-html-escaping-pre-existing-2026-08-04](check-identity-parity-json-html-escaping-pre-existing-2026-08-04.md)
- [branch_has_wip_roadmap-conflita-com-a-definition-of-done-2026-07-26](branch_has_wip_roadmap-conflita-com-a-definition-of-done-2026-07-26.md)
- [argparse-ansi-parity-gate-python313-2026-07-26](argparse-ansi-parity-gate-python313-2026-07-26.md)
- [ship-roadmap-dir-default-divergencia-2026-07-27](ship-roadmap-dir-default-divergencia-2026-07-27.md)
- [roadmap-move-nao-atualiza-frontmatter-2026-07-27](roadmap-move-nao-atualiza-frontmatter-2026-07-27.md)
- [barrier-git-authority-subagents-2026-07-29](barrier-git-authority-subagents-2026-07-29.md)
- [barrier-contract-xfail-false-positive-2026-07-29](barrier-contract-xfail-false-positive-2026-07-29.md)
- [barrier-gates-check-key-order-divergence-go-2026-07-29](barrier-gates-check-key-order-divergence-go-2026-07-29.md)
- [slash-commands-cross-runtime-content-drift-2026-07-29](slash-commands-cross-runtime-content-drift-2026-07-29.md)
- [rules-block-content-drift-3-clis-2026-07-29](rules-block-content-drift-3-clis-2026-07-29.md)
- [cobra-help-cmd-duplicate-registration-2026-07-29](cobra-help-cmd-duplicate-registration-2026-07-29.md)
- [update-harness-project-scope-json-gap-2026-07-29](update-harness-project-scope-json-gap-2026-07-29.md)
- [commander-nested-subcommand-duplicate-flag-drops-parent-2026-07-29](commander-nested-subcommand-duplicate-flag-drops-parent-2026-07-29.md)
- [node-tildeify-double-slash-home-2026-07-29](node-tildeify-double-slash-home-2026-07-29.md)
- [node-tildeify-trailing-separator-2026-07-29](node-tildeify-trailing-separator-2026-07-29.md)
- [update-project-scope-duplicate-generators-2026-07-29](update-project-scope-duplicate-generators-2026-07-29.md)
- [update-parity-gate-writes-real-claude-md-2026-07-29](update-parity-gate-writes-real-claude-md-2026-07-29.md)
- [agente-poluindo-repo-ao-rodar-cli-manualmente-2026-07-29](agente-poluindo-repo-ao-rodar-cli-manualmente-2026-07-29.md)
- [roadmap-new-gera-marcador-de-aceite-invalido-2026-07-31](roadmap-new-gera-marcador-de-aceite-invalido-2026-07-31.md)
- [security-drawer-marked-parse-unsanitized-stored-xss-2026-07-31](security-drawer-marked-parse-unsanitized-stored-xss-2026-07-31.md)
- [dashboard-serve-e-light-only-2026-07-31](dashboard-serve-e-light-only-2026-07-31.md)
- [seam-xss-drawer-armadilhas-de-verificacao-2026-07-31](seam-xss-drawer-armadilhas-de-verificacao-2026-07-31.md)
- [roadmap-from-req-ref-targets-exist-falso-positivo-2026-08-01](roadmap-from-req-ref-targets-exist-falso-positivo-2026-08-01.md)
- [adr-status-substring-livre-falso-positivo-2026-08-01](adr-status-substring-livre-falso-positivo-2026-08-01.md)
- [deteccao-de-status-de-adr-divergencias-entre-clis-2026-08-01](deteccao-de-status-de-adr-divergencias-entre-clis-2026-08-01.md)
- [validate-parity-gate-vacuo-e-go-sem-helper-unico-2026-08-01](validate-parity-gate-vacuo-e-go-sem-helper-unico-2026-08-01.md)
- [cenarios-de-falsificacao-quebram-em-refactor-do-alvo-2026-08-02](cenarios-de-falsificacao-quebram-em-refactor-do-alvo-2026-08-02.md)
- [falsify-s25-s26-go-quebram-apos-ml1d-squadval-2026-09-11](falsify-s25-s26-go-quebram-apos-ml1d-squadval-2026-09-11.md) — ML-1D adicionou `squadVal` a `roadmap.go`, zerando os count dos corrupt_literal de s25-go (chunk_6) e s26-go (chunk_4); chunk morre em setup, todos os rótulos subsequentes AUSENTE em cascata; retarget: inserir `squadVal,` após `reqPath,`/`content.REQPath,` nos dois cenários
- [falsificacao-fixture-vacua-contra-reversao-total-vs-parcial-2026-08-02](falsificacao-fixture-vacua-contra-reversao-total-vs-parcial-2026-08-02.md)
- [config-legacy-line-reader-sombreia-yaml-lib-no-validate-2026-08-02](config-legacy-line-reader-sombreia-yaml-lib-no-validate-2026-08-02.md)
- [falsificacao-config-parser-fixture-precisa-de-escalar-citado-2026-08-02](falsificacao-config-parser-fixture-precisa-de-escalar-citado-2026-08-02.md)
- [req-frontmatter-roadmap-nao-satisfaz-req-has-roadmap-2026-08-02](req-frontmatter-roadmap-nao-satisfaz-req-has-roadmap-2026-08-02.md)
- [python-update-run-project-bypassa-config-load-2026-08-03](python-update-run-project-bypassa-config-load-2026-08-03.md)
- [falsify-suite-locale-dependent-false-failure-2026-08-03](falsify-suite-locale-dependent-false-failure-2026-08-03.md)
- [req-move-statedir-hardcoded-roadmapdir-2026-08-04](req-move-statedir-hardcoded-roadmapdir-2026-08-04.md)
- [go-generateattentionscripts-cwd-vs-rootdir-2026-08-04](go-generateattentionscripts-cwd-vs-rootdir-2026-08-04.md)
- [thirdparty-provenance-key-domain-e-checksum-raw-vs-normalizado-2026-08-15](thirdparty-provenance-key-domain-e-checksum-raw-vs-normalizado-2026-08-15.md)
- [thirdparty-markers-fence-html-comment-immunity-bugs-2026-08-15](thirdparty-markers-fence-html-comment-immunity-bugs-2026-08-15.md)
- [integrations-manifest-write-precedes-persist-janela-de-registro-parcial-2026-08-16](integrations-manifest-write-precedes-persist-janela-de-registro-parcial-2026-08-16.md)
- [git-branch-guard-branch-create-heuristic-e-env-var-assignment-stripping-2026-08-17](git-branch-guard-branch-create-heuristic-e-env-var-assignment-stripping-2026-08-17.md)
- [git-branch-guard-noop-outside-project-fixtures-and-falsify-cwd-2026-08-17](git-branch-guard-noop-outside-project-fixtures-and-falsify-cwd-2026-08-17.md)
- [validate-global-guard-integrity-by-existence-makes-unisolated-home-systemic-2026-08-18](validate-global-guard-integrity-by-existence-makes-unisolated-home-systemic-2026-08-18.md)
- [global-guard-dedup-and-hook-resolvable-never-validate-hook-structure-2026-08-18](global-guard-dedup-and-hook-resolvable-never-validate-hook-structure-2026-08-18.md)
- [doctor-classifydoctor-silences-tampering-when-manifest-entry-removed-2026-08-19](doctor-classifydoctor-silences-tampering-when-manifest-entry-removed-2026-08-19.md)
- [git-branch-guard-case-block-extension-breaks-corrupt-literal-scenarios-2026-08-19](git-branch-guard-case-block-extension-breaks-corrupt-literal-scenarios-2026-08-19.md)
- [git-fetch-self-heals-forged-origin-head-and-tracking-refs-2026-08-19](git-fetch-self-heals-forged-origin-head-and-tracking-refs-2026-08-19.md)
- [forge-anchoring-branch-name-must-be-unconditional-not-cross-checked-2026-08-19](forge-anchoring-branch-name-must-be-unconditional-not-cross-checked-2026-08-19.md)
- [git-show-honra-refs-replace-por-padrao-2026-08-21](git-show-honra-refs-replace-por-padrao-2026-08-21.md)
- [deteccao-de-hook-relativo-nao-cobre-pwd-aspas-e-var-indefinida-2026-08-22](deteccao-de-hook-relativo-nao-cobre-pwd-aspas-e-var-indefinida-2026-08-22.md)
- [reason-do-guard-diverge-por-escaping-de-aspas-entre-python-e-go-node-2026-08-22](reason-do-guard-diverge-por-escaping-de-aspas-entre-python-e-go-node-2026-08-22.md)
- [classify-hook-anchorage-tilde-false-positive-2026-08-22](classify-hook-anchorage-tilde-false-positive-2026-08-22.md)
- [gh-stub-shebang-needs-usr-bin-in-restricted-path-2026-09-02](gh-stub-shebang-needs-usr-bin-in-restricted-path-2026-09-02.md)
- [copia-versionada-do-attention-signal-esta-obsoleta-e-sem-guarda-2026-09-02](copia-versionada-do-attention-signal-esta-obsoleta-e-sem-guarda-2026-09-02.md) — `scripts/trackfw-attention-signal.sh` da raiz é artefato **órfão**: o ML-1A corrigiu os 3 literais-fonte e a cópia versionada ficou sem o prefixo; nenhum teste ou gate compara a cópia com o literal (o parity roda em `mktemp -d`, os testes Go fazem `os.Chdir(t.TempDir())`), então auditar pelo arquivo de nome óbvio dá o veredito errado
- [isolação de home no Windows — o defeito é divergência de CANAL, não vacuidade](isolacao-de-home-no-windows-o-defeito-e-divergencia-de-canal-nao-vacuidade-2026-09-03.md) — corrige a nota de 2026-08-30: o shim de homedir (c88b81e) faz a produção ler `HOME` no Windows; o defeito é teste/gate resolvendo por `%USERPROFILE%`
- [check-validate-parity.sh pinava "bare relative path" para tilde aspeado](check-validate-parity-pina-bare-relative-path-para-tilde-aspeado-2026-09-04.md) — as 3 suítes unitárias concordam consigo mesmas mesmo com mensagem desatualizada; só o gate de paridade cross-CLI (mensagem byte-a-byte entre os 3 binários) detecta divergência real
- [A contagem de falhas de Windows do Go foi medida por padrão frouxo](contagem-de-falhas-de-windows-do-go-medida-por-padrao-frouxo-2026-09-04.md) — prefixo do `gh run view --log` quebra `^--- FAIL`; remedir a base antes de comparar delta
- [CRLF no frontmatter de agent-md: a fronteira de entrada não é uma função, são 7 por runtime](crlf-frontmatter-parser-precisava-de-normalizacao-em-7-funcoes-nao-1-2026-09-05.md) — normalizar só a função que o teste de Rota A exercita deixa Rota B quebrada; barrier/gates já era tolerante antes de qualquer fix (G1-bis)
- [TOCTOU: verificar pelo nome e usar pelo nome são duas resoluções](toctou-verificar-o-nome-e-usar-o-nome-sao-duas-resolucoes-2026-09-06.md) — a corrida entre `stat` e `open`, e por que o `fstat` no descritor é imune no POSIX
- [MSYS só converte caminho POSIX quando é o token inteiro, não embutido em string maior](msys-nao-converte-caminho-embutido-em-string-maior-2026-09-07.md) — `python3 -c "...open('$path')..."` falha no Windows mesmo com o arquivo lá; `HOME="$dir" bin` funciona porque é env var standalone; medido idêntico com/sem shim de PATH — não é causa de interpretador/binário
- [Matriz em job required-by-name fica pendente para sempre, não vermelho](matriz-em-job-required-por-nome-fica-pendente-para-sempre-2026-09-08.md) — `strategy.matrix` no job que carrega o nome do required status check faz o check esperado nunca reportar (`parity (0)`, `parity (1)`... nunca `parity`); correção é job de agregação com `needs:` + `if: always()` + step explícito de falha em `contains(needs.*.result, 'failure')`
- [O guard emitia JSON válido mas com schema errado — decision:block rejeitado pelo Claude Code](git-branch-guard-schema-decision-block-rejeitado-pelo-claude-code-2026-09-09.md) — corrige para `hookSpecificOutput`/`permissionDecision:deny`; a premissa da REQ sobre stderr vazio já estava desatualizada (commit `9411210`, anterior à REQ); sétimo sítio (`npm/src/validator/index.js`) escapa de grep estreito por caminho "generator"
- [Guard drenava stdin sem limite atrás de um discriminante errado (`-t 0`)](git-branch-guard-stdin-drain-timeout-em-vez-de-discriminante-tty-2026-09-09.md) — `[ -t 0 ]` separa terminal de pipe, não "vai receber EOF" de "não vai"; trocado por `read -t 2 -d ''` (builtin do bash desde 3.0, funciona em macOS/Linux/MSYS2 sem depender de `timeout(1)`)
- [Gate deriva sítio varrendo a árvore de trabalho em vez de `git ls-files`](gate-deriva-sitio-de-arvore-de-trabalho-em-vez-de-git-ls-files-2026-09-09.md) — CI reprova o ML-2A (9 sítios vs 7 locais): `pip install pypi/` materializa `pypi/build/lib/...` (ignorado por `.gitignore`) e `grep -rlas` sobre a árvore contava como sítio; trocado por `git ls-files`, exclui ignorado por construção; mesma causa raiz do issue #288 (`cp -r`), mecanismo diferente (varredura)
- [O bash resolve o que o processo filho nativo não resolve (Windows)](bash-resolve-o-que-o-processo-filho-nativo-nao-resolve-no-windows-2026-09-09.md) — `command -v git` do bash acha um `ln -s` sem `.exe` que `exec.Command`/`spawnSync`/`subprocess.run` não acham (CreateProcess+PATHEXT); `/usr/bin/git` não existe em nenhum Git for Windows e `/bin` é alias de `/usr/bin`; isolar `git.exe` num diretório morre com `STATUS_DLL_NOT_FOUND`; não chame a variável de `GIT_DIR`
- [Guarda que reporta ausência precisa distinguir "não achei" de "não consegui procurar"](guarda-que-reporta-ausencia-precisa-distinguir-nao-achei-de-nao-consegui-procurar-2026-09-10.md) — **6 instâncias em 2026-09-10** (#307, #274, #309, corretivo ML-R2c2, ML-2B núcleo, ML-2B baseline no CI): a guarda mede "não encontrei" e declara "não existe"; correção é sonda de sanidade do instrumento ANTES da pergunta, mensagem própria para cada estado, e em CI "não consegui procurar" é **fatal**, nunca aviso
- [Windows 8.3 short names quebram `path.relative` em fixtures que usam git](windows-8dot3-short-name-quebra-path-relative-em-fixture-git-2026-09-10.md) — `TEMP` do runner tem nome curto (`RUNNER~1`); `git rev-parse --show-toplevel` expande para nome longo; `path.relative(longo, curto)` produz garbage → `git cat-file -e` falha → "not committed" em vez de "content differs"; Go passa porque `filepath.EvalSymlinks` normaliza; correção: `fs.realpathSync(base)` (Node) / `os.path.realpath(base)` (Python) após `mkdtemp`
- [administration não é escopo de workflow GitHub Actions — schema rejeitado (0 jobs)](administration-nao-e-escopo-de-workflow-github-actions-schema-rejeitado-2026-09-11.md) — escopo válido de fine-grained PAT mas não de workflow permissions:; GitHub rejeita o schema inteiro (0 jobs criados), todos os required_status_checks ficam pendentes; yaml.safe_load passa mas actionlint detecta; medido: /protection retorna 401 anônimo e 200 com token pessoal, GITHUB_TOKEN em CI não confirmado
- [Gerador aplica template de consumidor ao produtor — doctor prescreve desfazer a correção](gerador-aplica-template-consumidor-ao-produtor-doctor-prescreve-desfazer-2026-09-17.md) — discriminante é `go.mod` (line-wise, não bytes.Contains+\n que falha em CRLF); AC8: gate exigia `@v<versão>` que o template de produtor não tem; "go install" em comentário YAML != run step (usar `run: go install` não `go install`); job-id `governance-go-install` é contrato com required_status_checks — não renomear

- [argv com shell=False não protege quando filho é cmd.exe — injeção via IPv6 zone ID no Windows](python-ipaddress-zone-id-metachar-windows-injection-2026-09-11.md) — list2cmdline só cita espaço/tab/aspas, não metacaracteres de cmd.exe; ipaddress.ip_address aceita qualquer zone ID; Fix: rejeitar '%' nos 3 CLIs (não validar zone ID, rejeitar a classe inteira); derivação confirma único sítio com valor controlável é o serve
- [Teste do gerador não prova AC da camada de comando](teste-do-gerador-nao-prova-ac-da-camada-de-comando-2026-09-11.md) — 13 testes Go verdes e o AC11 quebrado no binário: `roadmap new` tem DOIS caminhos (`--from-req`→`NewRoadmapFromREQ` com ordem certa; `--req`→`NewRoadmapFromContent` que ignora `content.REQPath`) e o teste exercitava só o que já funcionava; o ML **declarou a divergência em comentário no próprio teste** e mesmo assim ficou verde (Regra Dura de Reconciliação); Node e Python passaram no mesmo cenário; detecção: AC que fala de comando prova-se contra o BINÁRIO em projeto descartável, nunca contra a função geradora
- [Comparação de path de um lado só com `abspath` vs `realpath` em Python — macOS `/var` → `/private/var`](python-abspath-nao-resolve-symlink-macos-var-private-var-2026-09-12.md) — `os.path.abspath` normaliza mas NÃO resolve symlinks; `os.getcwd()` após `cd` symlink retorna canônico, mas caminho absoluto passado diretamente fica não-canônico; comparação `abspath(req_dir)` vs `abspath(req_path)` falha quando `req_path` aponta para `/var/folders/...` (não-canônico) e `req_dir` veio de `getcwd()` como `/private/var/folders/...`; fix: `os.path.realpath(os.path.abspath(...))` nos dois lados (espelha Go `EvalSymlinks` e Node `realpathSync`); mesmo padrão visto antes no Windows com 8.3 short names
- [Erro de expansão aritmética mata o laço e o script segue verde sob `set -euo pipefail`](erro-aritmetico-mata-o-laco-e-o-script-segue-verde-sob-set-e-2026-09-23.md) — `grep -c` + `|| echo 0` dá `$'0\n0'`; o job do censo ficou `success` publicando 2/8; guarda anti-reintrodução: `scripts/check-emitting-capture-fallback.sh` (ML-1B) — cobre `grep -c`/`--count` com fallback emissor, declara no cabeçalho o que não cobre
- [Rótulo de `FAIL` não pode virar exigência da guarda de conjunto](rotulo-de-falha-nao-pode-virar-exigencia-da-guarda-de-conjunto-2026-09-24.md) — 43 literais de `check-gates-falsify.sh` só existem em linha `FAIL` (caminho de falha): exigi-los deixaria o gate permanentemente vermelho — a regra é `OK`+`PROOF`; a guarda **não** detecta remoção no fonte (manifesto e runtime saem do MESMO arquivo), detecta rótulo que está no fonte e não apareceu no log; `weight_keys` (calibração) ≠ rótulo exigido (cobertura) — misturar quase dobrou a massa do LPT; artefato de shard antigo não se reaudita com gerador novo (148 falsos vs 33 reais por união)
- [Emissão de sucesso incondicional reporta `OK` para um braço que acabou de reprovar](emissao-de-sucesso-incondicional-reporta-ok-para-braco-que-reprovou-2026-09-24.md) — em `TRACKFW_FALSIFY_ENUMERATE=1` (o modo do censo de Windows, `continue-on-error: true`, apuração **por rótulo**) `falsify_fail_point` faz `return 0` e o `falsify_count_success`/`echo OK` que vinha depois do `fi` imprimia sucesso para o MESMO braço que reprovou; **39 sítios** no arquivo (30 da forma A + 9 da forma B, varredura estrutural conferida por 2 parsers), todos corrigidos na mesma REQ — sucesso no ramo `else`, ou `elif` com flag quando o braço tem várias checagens (🔴 `elif` puro engoliria o 2º diagnóstico); suíte inteira 273 OK / 0 FAIL antes **e** depois; 🔴 **limite:** `shard_N.actual` é montado com `^(OK|FAIL|PROOF)`, então a guarda de conjunto só discrimina quando o rótulo de falha **difere** do de sucesso — nos demais o ganho é a consistência de `^OK`/`^FAIL` do censo; 🔴 `vacuity-guard` **não pode** receber rótulo de sucesso (bloco pulado em chunk por `declare -f __falsify_timing_mark` → exigência nunca emitida → gate permanentemente vermelho, medido); `setup*` fora, confirmado quebrando o setup do Cenário 69; **lateral:** o `CHUNK_ABORT` do ML-2A só cobre o chunk que recebeu o Cenário 18 (7 dos 8 shards do censo ficam sem diagnóstico) e `grep -oF | wc -l` sem casar mata o chunk por `pipefail`
- [O `git` sai 128 no Windows porque o corpus de testdata estoura MAX_PATH](o-git-sai-128-no-windows-porque-o-corpus-de-testdata-estoura-max-path-2026-09-24.md) — falsificado nas duas direções com `core.longpaths`; a falha é marginal e o nome do `mktemp` decide; não é o MSYS, que aguenta 916 chars
- [Trap dentro de bloco de cenário cobre um subconjunto de chunks que depende da partição](trap-dentro-de-bloco-de-cenario-cobre-subconjunto-que-depende-da-particao-2026-09-24.md) — `gen-falsify-chunks.py` copia para todo chunk só `lines[:prelude_end]` + segmentos de suporte, então handler escrito dentro de um cenário existe só no chunk que recebeu aquele cenário; medido em N=4/8/12/16/60: **sempre 1 chunk com trap**, e a partir de N=12 o sítio que matou o `chunk_0` do censo cai num chunk **sem** trap — em N=8 os dois coincidem e o defeito fica invisível (por isso duas medições corretas discordavam); correção é instalar no preâmbulo real, conferindo `prelude_end_line` pelo próprio gerador (1431 → 1492) com `total_segments`/`fused_units`/234 rótulos idênticos; 🔴 preservar `ERR` e **não** `EXIT` (o slot de EXIT é do `rm -rf "$WORK"`) e **sem** `set -E` (errtrace faria toda falha esperada de gate escrever `CHUNK_ABORT` no log do subshell); 🔴 **terceiro achado, o que mais custa tempo:** `trap … ERR` dispara **mesmo com `set +e`** (medido: `$-`=`huB` dentro do handler), e este script usa `set +e` em dezenas de blocos para capturar comando que deve falhar — o trap no preâmbulo gerou 2 `CHUNK_ABORT` falsos num chunk `rc=0`/36 OK; guarda é `case "$-" in *e*) ;; *) return 0;; esac`; **segundo achado na mesma nota:** `n=$(grep -oF … | wc -l)` sob `set -euo pipefail` mata o chunk quando não casa (mesma raiz da Wave 1, outra forma) — corrigir com `$( { grep … || true; } | wc -l )`, nunca `|| echo 0` nem `${VAR:-0}`; o gate do ML-1B **não** cobre (exige fallback emissor; aqui não há fallback) e cobrir exige discriminante irmão
- [Nem todo `grep` em captura é defeito — a posição sintática do sítio decide](nem-todo-grep-em-captura-e-defeito-a-posicao-do-sitio-decide-2026-09-24.md) — dos 10 candidatos do ML-2E sobraram **5 defeituosos e 5 isentos**, e as listas não são sublistas: três isenções são **estruturais** e medidas em `bash 5.3` (substituição em **posição de argumento** tem o rc descartado; `v=$( … ) || true` guarda **fora** do corpo, que é o que o filtro não via; corpo em `bash -c` não herda `pipefail`) e uma é **semântica** (não-casamento já excluído por checagem anterior que dá `exit 1`) — 🔴 escrever essa última na forma estreita, porque `grep | head` ainda pode sair **141** por `SIGPIPE` num corpus grande; assinatura do defeito real é a linha seguinte ser um `if [[ -z "$v" ]]` com diagnóstico próprio — o ramo está **morto hoje**; `check-manifest-version-gate.sh:113` tem **dois** greps letais (o 2º mata quando só há `## [Unreleased]`, estado normal de meio de ciclo) e por isso a falsificação tem **três** direções; `check-annotations.yml:80` é posição **final**, não não-final — o `set -e` sozinho mata e a morte acontece no **subshell** do `while` que é elo final de pipeline, deixando os jobs restantes sem medição; a forma **irmã sem cano** (`v=$(grep … arq)`, sem precisar de `pipefail`) é invisível ao filtro — **5 sítios** vivos nomeados; e as 4 classes de isenção são o que o gate do ML-2H não pode acusar (1–3 sintáticas, 4 não decidível)
- [O `|| true` que vira aprovação vácua — a forma sem cano e o que vem **depois** da atribuição](o-ou-true-que-vira-aprovacao-vacua-a-forma-sem-cano-e-o-que-vem-depois-da-atribuicao-2026-09-24.md) — fecha a forma `v=$(cmd …)` **sem cano**, que mata pelo rc do comando sozinho sem depender de `pipefail`. 🔴 O teste de bolso da nota irmã ("a linha seguinte trata o vazio?") **inverte** quando a medição é uma **comparação de duas capturas**: em `check-install-version-pin.sh` as duas ficam vazias, comparam **iguais** e o cenário emite `OK` — medido, a guarda de rc **isolada** troca morte muda por **aprovação vácua**, que é pior; a correção exige guarda de rc **+** guarda de não-vacuidade com rótulo, e a falsificação tem **três** braços. Teste corrigido: *"o código **depois** da atribuição distingue vazio de valor medido?"*. Duas classes novas de isenção estrutural medidas em `bash 5.3` — **arquivo sem `set -e`** e **`local v=$(cmd)` na mesma linha** (rc é o do `local`, sempre 0; não vale com `local` em linha separada). Varredor por **família** (não pelo token `grep`) acha **13** contra os **5** do ML-2G, e os 8 novos são `command -v` (4), `find` (3) e um `grep` que é o falso positivo — o token estava certo, a família é que era estreita; `command -v` que alimenta shim gerado é **isento**, `|| true` ali fabrica instrumento quebrado. Placar **5 corrigidos / 7 isentos (3 deles em arquivos fora do escopo de escrita) / 1 falso positivo de varredor linha-a-linha (substituição multi-linha)**
- [`sed -n` não tem rc para propagar — o único discriminante é o conteúdo, e ele é o **basename**](sed-n-nao-tem-rc-para-propagar-o-unico-discriminante-e-o-conteudo-e-ele-e-o-basename-2026-09-24.md) — terceira da série (ML-2J). Fecha a forma em que **não existe rc**: `sed -n`/`awk`/`cut` saem **0** mesmo sem produzir saída, logo a guarda de rc é inútil **por construção** e `{ … || true; }` ali é decorativo — as três variantes da nota irmã colapsam em uma. 🔴 **Achado principal:** a guarda de não-vacuidade tem de incidir sobre a **expressão que o `if` lê**, não sobre a captura — a comparação real era `"${DEST_BARE##*/}"`, e um `DEST` terminado em `/` tem captura **não-vazia** e basename **vazio**, medido: a guarda copiada do padrão do `URL` passaria batido e o `OK` vácuo voltaria. A forma perigosa é estreita — **duas capturas do mesmo produtor**; comparar contra literal não-vazio é **auto-discriminante** e dispensa guarda. Censo por par captura↔comparação em `scripts/*.sh` + `.github/**` + `Makefile`: **67 brutos = 4 corrigidos / 63 isentos** em 5 classes (18 terminam em `wc`, que sempre emite número; 20 já têm `-z` na linha seguinte). Isenção **semântica** registrada em `check-agent-namespace-union.sh:904,905` (laço acima já faz `fail`+`exit 1`). 🔴 **Três cegueiras do varredor escritas**, incluindo a que ele teve contra o próprio alvo conhecido: expansão de parâmetro na comparação não casava na primeira passada.
- [Cinco defeitos do **instrumento** ao transformar as 6 classes de isenção num gate](cinco-defeitos-do-instrumento-ao-transformar-as-6-classes-em-gate-2026-09-24.md) — fecha a série (ML-2H, `scripts/check-unguarded-capture-rc.sh`). 🔴 **Quatro dos cinco falham para o lado do falso negativo**, que fica verde e ninguém olha: (1) contagem de parênteses **não ciente de aspas** engole a substituição inteira quando o padrão do `grep` tem `(` literal — `quality.yml:1254` não aparecia **nem como candidato**; (2) o ERE `set -o pipefail` **não casa `set -euo pipefail`** (o `o` vem empacotado), e com isso a classe 3 isentava **13** sítios em vez de 3 — o gate nascia sem cobrir a forma com cano; (3) `errexit`/`pipefail` são estado **sequencial por escopo**, não `grep` no arquivo — `check-install-version-pin.sh` desliga errexit entre `:465` e `:473`, e o bloco `run:` do GitHub é `bash -e {0}` por padrão mas `bash -eo pipefail {0}` com `shell: bash`; (4) isenção por **palavra-chave no prefixo** (`if`/`while`) é larga demais — `if [ -n "$x" ]; then v=$(grep …)` tem o rc PROPAGANDO e era isentado (0 sítios hoje, correção preventiva, falsificada nos braços Q e H); (5) `local s="$1" n=${#s}` **aborta sob `set -u`** (o builtin declara todos os nomes antes de atribuir) — a varredura devolvia 0 candidatos e a guarda com piso 0 reportava `OK vacuidade: 0 >= 0`, o defeito da REQ anterior reproduzido por acidente. 🔴 A guarda de obsolescência das alegações nascia vermelha porque a contagem morria no **subshell do `$( )`** — mesma raiz desta REQ, terceira roupa; regra: **função com efeito colateral em global não pode ser chamada dentro de `$( )`**. Família de **candidatos** (larga: `command -v`/`find`/…) ≠ família de **violação** (estreita: só `grep`) — sem a larga, as classes 4 e 5 ficam **sem testemunha impressa** e `check-orphan-gates.sh:86` (que usa `find`) seria nomeado testemunha por razão errada. Classe 5 (`local v=$(cmd)` na mesma linha) **não tem sítio real**: medido 0, provada por injeção + contraprova (`local` em linha separada **reprova**). Fechamento: 108 candidatos / **0 violações** / 69 arquivos / piso 60, conferido por segundo caminho (109); **exatamente 2** sítios chegam ao estágio de violação — os `alfa_ln`/`zulu_ln` previstos, alegados como classe 6 na forma **estreita** (pré-validação **e** saída pequena demais para `SIGPIPE 141`)
- [Cross-check de dois instrumentos **concorda em zero sem medir**](cross-check-de-dois-instrumentos-concorda-em-zero-sem-medir-2026-09-24.md) — ML-2K, fecha a série. O par `grep`/`awk` da apuração do censo é **auto-concordante no vazio**: log presente sem nenhuma linha `^OK`/`^FAIL` faz as 4 contagens valerem 0, o cross-check **concorda sem medir** e o shard morto entra no total como `delta=-113 ▼` — **a maior melhora da tabela** — com `Shards completos: 8/8` e rc=0 (medido no bloco pré-ML). 🔴 A correção **não é um terceiro contador** (mais instrumentos sobre produtor vazio continuam concordando) e sim **testemunha de medição** derivada das 4 capturas existentes; não-vacuidade é **pré-condição** da comparação, nunca resultado. Casos alcançáveis medidos: log 0 bytes e log com texto sem veredito entram no **mesmo ramo** (discriminante idêntico), e log **ilegível não chega à guarda** — o `awk` sem `|| true` mata o step na atribuição (`rc=2`, medido com `chmod 000`), morte ruidosa, sem ramo morto. Falsificação em 3 direções com o bloco `run:` **extraído por `yaml.safe_load`**, incluindo caminho feliz **byte-idêntico** ao de `git show HEAD`. 🔴 **Segundo achado:** migrar as alegações de classe 6 da tabela `ALLEGATIONS` para o marcador inline esvazia a tabela, e ela era a **única fixture do braço P do Cenário 199** — a guarda de obsolescência passa a iterar ZERO entradas e reporta verde **sem examinar nada** (o defeito que o próprio gate nomeia); marcador e entrada de tabela são **mutuamente exclusivos** (o inline é consultado antes e a entrada viraria obsoleta), e o marcador precisa estar acima de **cada** sítio porque `alfa_ln`/`zulu_ln` são adjacentes
- [bash consome stdout de `python3` e recebe um `\r` invisível no Windows](bash-consome-stdout-de-python3-e-recebe-cr-invisivel-no-windows-2026-09-23.md) — mesmo rótulo emitido *e* acusado ausente; `grep -qxF` quebra, `grep -qF` passa; `PYTHONIOENCODING` não resolve
- [`go test -overlay` com chave em caminho POSIX é ignorado **em silêncio** no Windows](overlay-json-com-caminho-posix-e-ignorado-em-silencio-pelo-go-no-windows-2026-09-24.md) — mesma família do caminho POSIX-MSYS, **outra superfície**: `printf` de `$GO_API_FILE`/`$VULN_GO` para dentro de `overlay.json` (`scripts/check-serve-api-file-security.sh:96`); chave que não casa nenhum arquivo do build **não é erro** para o Go — o teste roda contra o fonte **correto**, passa, e o braço de falsificação do AC6 conclui "passou na versão vulnerável", apontando o diagnóstico para o teste em vez do `printf`; A/B na mesma máquina: chave POSIX → `ok`, chave por `cygpath -m` → `--- FAIL … esperado 403, obteve 200; corpo vazou segredo`; correção por `argv` **não serve** (não há argv) — regra de bolso: caminho que sai do bash do MSYS e entra em programa Windows nativo por **arquivo de configuração** precisa de `cygpath -m`; 🔴 o gate do ML-1B **não vê** este sítio (varre corpo Python, aqui é `printf` para JSON); em `ubuntu-latest` POSIX é a grafia nativa e o braço passa (run `36023336663`) — defeito **exclusivo do Windows**; achado irmão na mesma nota: em `/tmp` montado `ntfs (binary,noacl,posix=0)` o `chmod 644` deixa o arquivo **755** e `[ -x ]` verdadeiro, então a fixture `pin7-noexec` de `check-validate-rule-pins.sh` não é construível nesse Windows (**permissão**, não tradução de caminho)
- [O MSYS converte **env var e `argv`**, mas **nunca conteúdo de arquivo** — e o G4 nasce nessa assimetria](msys-converte-env-e-argv-mas-nunca-conteudo-de-arquivo-2026-09-24.md) — ML-1A (G4) **refuta a premissa do ML-0A**: o `HOME` que chega ao Go **tem** letra de unidade (o MSYS converte a env var ao lançar processo nativo), `hasWindowsDriveLetterPrefix` é verdadeiro e a conversão `\`→`/` **já acontece** (`NORM_COMP="C:/Users/Lab/…"`) — a divergência é de **espaço de nomes** (`/tmp/…` do heredoc do fixture vs `C:/Users/…/Temp/…` do `filepath.Join`), não de separador. 🔴 A/B com o **binário real**: mesma fixture, só mudando a grafia gravada no `settings.json` global → grafia MSYS **grava** a entrada de projeto (reproduz o FAIL do censo), grafia nativa por `cygpath -m` **pula** (dedup dispara). Logo o produto está certo e o G4 sai de **D (produto)** para **harness** (`scripts/check-gates-falsify.sh:4722-4726` e `:4836`). O elo em aberto do parecer está respondido: `readGlobalHookJSON` **sucede** (`err=<nil>`) — quem decide é a comparação. 🔴 Nenhuma regra de string casa as duas grafias (a diferença é de *montagem*), então toda "correção" que as faça casar é afrouxamento semântico — reforça o alerta contra comparar por *basename*. A presunção de "não há cobertura Go" **cai**: há 22 casos em `guard_path_normalize_test.go`, e um deles **fixa a forma como intencional** (`scripts\guard.sh` untouched) — teste que defende o defeito na revisão. Mesma família de [[overlay-json-com-caminho-posix-e-ignorado-em-silencio-pelo-go-no-windows-2026-09-24]], direção invertida
- [Fixture que cruza a fronteira MSYS controla **as duas pontas a partir de uma string só**](fixture-que-cruza-a-fronteira-msys-controla-as-duas-pontas-a-partir-de-uma-string-2026-09-24.md) — ML-1C/ML-1D (G4): o **remédio** da assimetria de [[msys-converte-env-e-argv-mas-nunca-conteudo-de-arquivo-2026-09-24]], com as duas armadilhas do remédio óbvio. 🔴 Converter **só** o arquivo deixa o MSYS na fronteira e depende de `cygpath -m` ser byte-igual à conversão de env var do MSYS — **não é**: VM ARM64 dá nome longo, censo x64 dá **8.3** (`C:/Users/RUNNER~1/…`), então o fixture passaria na VM e seguiria vermelho no CI. Remédio: converter **uma vez** e usar a **mesma string** nos dois lados (JSON *e* `HOME`); em POSIX o helper devolve a entrada inalterada, então o determinismo sobrevive por construção. 🔴 Segunda armadilha: `cygpath` **colapsa** o `//` que o braço 4 mede — converter a base e injetar o `//` depois, **assertando** a sobrevivência com âncora no **segmento**, nunca em `//` solto (um prefixo `C:/` satisfaria a âncora ingênua). **Achado colateral:** `detection-catches-regression` estava **`OK` antes e depois** — verde **vacuoso**, porque com `MATCH=false` a entrada reaparecia independente de o binário estar corrompido; num par baseline/detecção que compartilha fixture, detector verde com baseline vermelho **não prova nada**. ⚠️ Chunks de `gen-falsify-chunks.py` **têm de ser gerados dentro do repo** (`ROOT_DIR=dirname/..`) ou morrem na linha 32 com log de **uma linha**. Veredito do ML-1D: `scripts\guard.sh` intocado é **comportamento desejado** (sem âncora, `\` é byte de nome de arquivo), caso **mantido e renomeado** — e **nenhum** caso daquela tabela podia ter pego o G4, que diverge por **montagem**, não por sintaxe
- [O guard **aprova** quando não conseguiu ler o comando — e a culpa é do orçamento **total** do `read -t`](guard-aprova-quando-nao-conseguiu-ler-o-comando-orcamento-total-do-read-2026-09-24.md) — ML-1B (G5). O dreno `IFS= read -r -t 2 -d ''` tinha orçamento **total**, que é dependente do **tamanho** do payload: medido no Git-Bash 5.3.15 do Windows, 200000 bytes de um **pipe** levam **13,6 s** (`-t 600` → `rc=1`, `len=200000`) contra **0,151 s** no macOS — ~**14,7 KB/s** vs ~1,3 MB/s, porque o `read` consome fd não-seekable 1 byte por `read(2)` e a syscall do MSYS é ~90x mais cara; com `-t 2` o guard lia **25397 de 200000** (`rc=142`) e **saía 0**, aprovando um comando que não leu — a única evidência era `SIGPIPE 141` **no escritor**. 🔴 **A semântica de `-d '' ` está certa** (lê até EOF quando há tempo) — fecha a dúvida deixada em aberto pelo ML-0A. 🔴 **Aumentar o `-t` é maquiagem** (passa em 200 KB, falha em 400 KB): a correção troca a **natureza** do orçamento de **total** para **ocioso** (renova a cada byte), que é independente de tamanho e ainda corta o escritor-que-nunca-escreve — o que pendurou `make quality` por 1h05. **Segundo achado, o que custa tempo amanhã:** o comentário do próprio código afirmava, como medição, que `read -t` preserva o prefixo no timeout em "bash 3.2 e 5.3" — **falso para o 3.2** (padrão do macOS): medido `rc=1` (não 142) e variável **vazia**, então no bash 3.2 **timeout é indistinguível de EOF** e o fail-closed por `rc>128` é condicional à plataforma. Contrato novo: **fail-closed** com deny+`exit 2`, **depois** do no-op da ADR-2026-08-17 e com **argv isentando** (`$TRACKFW_GIT_COMMAND` **não** isenta — payload truncado pode render comando não-vazio e errado). A/B na mesma VM: antigo `writer_rc=141` + `guard_rc=0` silencioso; novo `writer_rc=0` e `guard_rc=2` visível. ⚠️ Pós-correção 200 KB deixa de ser "ilegível" (só lento, 13,4 s) — a direção 2 da falsificação teve de virar **escritor travado**. O dreno vive em **4 sítios byte-idênticos** (scaffold.go, validator reference, `scripts/trackfw-git-branch-guard.sh`, e o `corrupt_literal` do Cenário 65)
- [Rodar **um único** cenário de `check-gates-falsify.sh`, e provar que a sabotagem dele não é vácua](rodar-um-unico-cenario-de-check-gates-falsify-e-provar-que-a-sabotagem-nao-e-vacua-2026-09-24.md) — ML-1E (G5). **Não existe filtro de cenário**: `TRACKFW_FALSIFY_ENUMERATE=1` é modo de *contagem*, não seletor — o caminho é usar `gen-falsify-chunks.py` como seletor (`grep -l <marcador>` nos chunks; o fechamento por variável já traz o cenário do qual o alvo depende — medido: N=60 pôs os Cenários 64 e 65 no mesmo `chunk_11`, porque o 65 consome `$T64_BASE_OUT`). 🔴 `TRACKFW_ROOT_DIR="$PWD"` é obrigatório (sem ele `ROOT_DIR` resolve errado e morre em `cp: .../cmd/.`), e `bash` explícito porque o shell é `zsh`. 🔴 **Chunk verde não prova que a sabotagem mede algo:** o contra-braço é trocar a sabotagem por **identidade** (literal por ele mesmo) e exigir que o braço de detecção **reprove** — medido: real → `chunk_rc=0`/`escritor_erro=1`; identidade → `chunk_rc=1`/`FAIL … cenário vácuo`. O literal do `corrupt_literal` **é o contrato** e já quebrou o gate 2× (abort `expected exactly 1 occurrence, got 0`, não FAIL): contar à mão antes de aplicar, e escapar `$?` como `\$?` (aspas simples não servem — o literal contém o `''` do `-d ''`). ⚠️ `TestGitBranchGuardScriptReference_MatchesGenerator` compara **duas constantes Go**, não o arquivo em disco — quem prova `scripts/trackfw-git-branch-guard.sh` é a regra `git_branch_guard_script_integrity` do `validate`, falsificada aqui nas duas direções
- [`chmod` que não retira o bit torna o braço de baseline **vácuo** — e o `SKIP` reconstrói a vacuidade um nível acima](chmod-que-nao-retira-o-bit-torna-o-braco-de-baseline-vacuo-e-o-skip-reconstroi-a-vacuidade-2026-09-24.md) — ML-2A (G3). A vacuidade **se reproduz em darwin**: imagem FAT32 via `hdiutil` (esparsa, ~6 GB — 200 MB morrem em `no space left` compilando o binário sabotado) + `TMPDIR` apontado para ela; medido: código de `HEAD` **com o braço de detecção removido** fica **verde inteiro** (rc=0), código novo fica 2× `FAIL` nomeado (rc=1), e em APFS os dois braços seguem `OK`. 🔴 `SKIP` **não era saída disponível**: o driver colhe `^(OK|FAIL|PROOF)`, então a linha `SKIP` vira *"rótulo esperado AUSENTE"* (parece chunk morto) **e sai 0** — a categoria C reconstruída dentro da guarda; `FAIL [falsify/<rótulo>]: …` satisfaz a exigência de rótulo **e** reprova, e por isso os **dois** rótulos são emitidos no ramo de fixture inconstruível. Falhar alto é autorizado porque o `windows-census.yml` é `workflow_dispatch` + `continue-on-error` (nunca bloqueia merge) — se virar bloqueante, reabrir. Testemunha de CI (run `36036473391`, linha 4187, `-rwxr-xr-x` **depois** de `chmod 0644`) fecha o **efeito** da #421 em x64, mas **não** a atribuição ao `noacl`: falta uma linha de `mount`/`findmnt -T "$TMPDIR"` no censo. `icacls` rejeitado por **custo e imensurabilidade daqui**, não por mecanismo. Lateral: o braço de baseline usa `$ROOT_DIR/bin/trackfw` **committado** — atesta `bin/`, não a árvore
- [O rótulo de sucesso **fora do ramo de aprovação** são **18**, não 4 — e o detector nomeado acha 9](o-rotulo-de-sucesso-fora-do-ramo-de-aprovacao-sao-18-nao-4-e-o-detector-nomeado-acha-9-2026-09-24.md) — ML-0B. 🔴 O detector do handoff (*"a Forma B não chama `falsify_count_success`, logo `^OK` ≠ tally"*) é **falso para 9 dos 18 sítios**: eles chamam `falsify_count_success` na linha imediatamente acima do `echo OK` incondicional, então os dois contadores andam juntos **inclusive quando o cenário reprova** — e o piso de vacuidade `FALSIFY_SUCCESS_FLOOR` é **inflado por reprovação**. A enumeração que funciona é **estrutural** e precisa do **fecho transitivo dos envelopes** que alcançam o ponto de reprovação e **retornam** (`assert_*`, `build_go_or_fail`, `run_go_guard_dump`, …): sem o fecho, 12 candidatos; com ele, 27 → **18 verdadeiros / 9 falsos positivos** já corrigidos pelo padrão `_falsify_arm_fail_<linha>`+`elif`. Duas subclasses, e a segunda é nova: **guard de setup** (10) e 🔴 **sumário de braços** (8: `call-site-pin`, `crlf-normalize`, `emitting-capture`, `unguarded-rc`, `write-containment`, `ci-workflow-self-governance`, `roadmap-ref-stale-state/{go,python}`) — um `assert_fails_with` que reprova **retorna 0** por construção e o sumário logo abaixo afirma *"os N braços provados"*. A correção **preserva o `echo "OK   [falsify/…]"` literal** (é dele que `gen-falsify-chunks.py`/`ECHO_LABEL_PAT` colhe o conjunto exigido pela guarda do driver — trocar por helper apagaria o rótulo do conjunto) e emite `FAIL [falsify/<mesmo rótulo>]` no ramo suprimido, senão vira *"rótulo AUSENTE"*. 🔴 **Achado independente e pré-existente:** o bloco `exit 1` do modo de enumeração está **antes do Cenário 200** no fonte, então no censo de Windows qualquer reprovação no mesmo chunk apaga os **9 rótulos `interp-path/*`** sem dizer por quê. ⚠️ Cópia sabotada de gate precisa de raiz plausível (`ROOT_DIR=dirname/..`): fazenda de symlinks para o parity, `TRACKFW_FALSIFY_SCRIPT` para o falsify
- [O GitHub ignora palavra-chave de fechamento em zona de CÓDIGO e a honra em zona NÃO-CÓDIGO](github-ignora-keyword-de-fechamento-em-zona-de-codigo-e-honra-em-zona-nao-codigo-2026-09-24.md) — ML-0C, 7 PRs sonda (`closingIssuesReferences`): cerca **#428**, code span **#429** e bloco indentado 4 espaços **#431** devolvem `[]` (**não fecha**); blockquote **#432**, célula de tabela **#433** e aspas retas **#434** devolvem `[430]` (**fecha**); controle em prosa **#427** = `[426]` — 🔴 sem o braço de controle um `[]` é indistinguível de instrumento quebrado. Consequência: o mascaramento de zona em `check-pr-closing-keyword.sh` é **por balde** — zona de código sai dos **dois** matchers (é o `blank_code` de hoje, e está **correto**: a "forma 8" do ML-0B **não é defeito**, `esp`=1), zona não-código sai **só** do matcher português; passe duplo em zona de código instalaria **falso negativo** (silêncio sobre corpo que declara fechamento e não fecha). Refutou **duas** presunções do mesmo parecer anterior, em direções contrárias (forma 8 como FP; e o "5 de 5" de §3.5-iv, que é **3 de 5**). 🔴 **Não extrapolar balde por analogia** — comentário HTML, `<pre>`, aspas curvas, `~~~` e cerca com linguagem seguem **não medidos**, e foi a analogia que produziu as duas presunções erradas; premissa sobre sistema externo que decide a **direção** de um fix não pode ficar premissa: 357 PRs de corpus não decidiam, uma sonda descartável decidiu
- [O censo de sítios mudos por NOME perde o que está dentro do próprio `pathguard`](o-censo-de-sitios-mudos-por-nome-perde-o-que-esta-dentro-do-proprio-pathguard-2026-09-25.md) — ML-7B: os mudos eram **4**, não 3 — `GuardedWrite` recusava em silêncio e o censo o isentou **antes** de medir (regra: isenção diz onde a correção mora, não onde o defeito pode estar); adaptador que delega **e** re-embrulha dobra a mensagem (`refusing write to X: refusing write to X: …`) e passa em todo `strings.Contains`; teste de sítio mudo degenera verde quando a fixture não torna a guarda alcançável (`if root, err := projectRoot(); err == nil`) — braço de controle tem de exigir stderr vazio **e** o arquivo escrito; "não fatal" é controle de fluxo e não licença para gramática própria (5ª e 6ª gramáticas eram isso); e identidade por construção exige const **sem o verbo final**, porque `go vet` reprova format string não constante e `Fprintf` não aceita `%w`
- [Resolver o root só vale quando o **outro operando** se move junto](resolver-o-root-so-vale-quando-o-outro-operando-se-move-junto-2026-09-25.md) — ML-8A, fecha o #402. 🔴 **O exemplar citado no handoff (`discover.go:31-38`) NÃO satisfazia o P2**: `buildEnv` grava `env[nome]` só na **primeira** ligação, então `resolvedCwd := cwd` seguido de reatribuição condicional é correto em runtime e **invisível** ao analisador — o root de guarda tem de vir de **uma** atribuição a partir de **uma** chamada de resolvedor (foi por isso que nasceu `pathguard.ResolveRoot`, colapsando 5 cópias do idioma). Discriminante que decide se um sítio é corrigível: **o outro operando se move junto?** Alvo construído por `filepath.Join` a partir do root, na mesma função → mecânico; alvo vindo de fora → é **decisão**, não conserto de argumento — e resolver o alvo por `EvalSymlinks` é proibido (autorrefutação do ML-6A: apaga o symlink que a guarda existe para recusar). Os dois que reprovaram o teste, medidos: `Manager.resolve` (destino absoluto do usuário aceito verbatim → `ResolveRoot(root)` reprova **7 testes**, ex. `TestClaimOrigin_LegacyManifestReadsAsCatalog`) e `UpdateHarness` (`home` é gravado **verbatim** dentro de `.claude/settings.json` & cia → resolver muda o **conteúdo** gerado, **15 testes** fixam a forma lógica). Achado 3: `root-aliased` é a metade da Wave 8 que ninguém contou — `internal/discover` guardava `Join(root,…)` e **escrevia** `Join(rootDir,…)`; 13 dos 16 caíram com troca de variável. Fechamento: Clean 16→**0**, sem-proveniência 22→**3**, aliased 16→**3**, P1 15→15
- [A extensão escolhida para esconder do gate esconde também do `.gitattributes`](a-extensao-que-esconde-do-gate-esconde-tambem-do-gitattributes-2026-09-25.md) — ML-9B, corretivo de CI do PR #441. O `.go.txt` do corpus pré-fix (escolhido para ficar fora do build **e** do `find -name '*.go'` do gate) o tirou também da regra `*.go text=auto eol=lf`: no Windows o checkout converte e os 16 sha256 do MANIFEST acusam "evidência modificada" — **instrumento, não produto**. Reproduzido em macOS com `git -c core.autocrlf=true checkout-index` (2172 → 0 linhas CRLF), 🔴 com braço-contrário obrigatório na mesma árvore (`docs/cli-parity.md` = 7451, `pathguard.go` = 0), porque zero sozinho é indistinguível de "a conversão nunca disparou". 🔴 `-text` ≠ `text eol=lf`: `text` normaliza CRLF→LF **no check-in**, e uma edição acidental seria regravada em LF com o sha256 intacto — adulteração invisível para o instrumento feito para detectá-la; a linha vai no **fim** do arquivo (último padrão que casa vence). 🔴 **Refutação medida** da explicação óbvia do golden: `Render` do `subagent` **não** passa por `NormalizeCRLF`, o `got` sai COM CRLF, os dois lados se cancelavam em parte (3 subtestes → 1 depois do pin) — causa remanescente é **de produto**, não de checkout. E a asserção de caminho quebrou porque o **emissor** mudou (relativo → absoluto do SO no ML-7B): `filepath.FromSlash`, nunca `Contains("symlink")` genérico, que passaria com o caminho errado
- [O prefixo `ROADMAP-` é token de TODO roadmap — e o `Contains` de hoje rejeita **29 das 205** branches governadas](o-prefixo-roadmap-e-token-de-todo-roadmap-e-o-contains-rejeita-29-das-205-2026-09-26.md) — ML-3A (fecha o #273). 🔴 A direção **restrito demais** é maior do que o issue mediu: sobre 201 roadmaps e 205 branches históricas, `strings.Contains` **rejeita 29** (14%, contra ~9% do #273), e **14** delas casam o roadmap certo por ≥2 tokens de conteúdo — os 176 aceitos são população **sobrevivente** (o autor do #273 renomeou a branch para caber). 🔴 Tokenizar o **nome do arquivo cru** dá token grátis: `roadmap` casa **175 de 201** cru contra **16** com o prefixo estrutural removido, e a aceitação espúria concreta é `feat/roadmap-list` × `ROADMAP-…-req-move-list-…` com tokens `[roadmap, list]`; o remédio é remoção **posicional** (`ROADMAP-`/`REQ-`/`ADR-` + data ISO + `.md`), **nunca** lista negra — um roadmap cujo **título** contém "roadmap" precisa manter esse token (o `req` do mesmo par é título e sobrevive). Limiar **2 é forçado dos dois lados**: o par do #273 compartilha **exatamente 2** tokens (`divida`+`acervo`), logo `N≥3` reabre o falso-negativo, e com `N=1` a relação aceita **205 de 205** e um termo genérico admite ~10% do acervo (`req` 18, `gate` 20). 🔴 **"Falso positivo" tem dois sentidos** e medir o errado dá falso verde: como a relação é um **OR**, tokens extras **não mudam veredito** de quem já passava — o nível de **veredito** é o AC15 (regressão 0 **por construção**, confirmada nas 205 × 4 limiares × 2 tokenizações), o nível de **cardinalidade** é o sinal da etapa 2 restritiva. Slug **vazio** casa nada (exceção declarada da D4; inerte no acervo: 0 das 205 normaliza para vazio). ⚠️ **Armadilha de gate:** `check-validate-rule-pins.sh` reusa `pin6`/`pin7`/`pin8` no **Bloco 3**, e continuar a numeração local do Bloco 2 **passa com rótulo duplicado** sem nenhuma detecção (renomeados para `pin2b`/`pin2c`/`pin2d`)
