---
name: v2.5.2-traceid-by-agent
title: "v2.5.2 — Fix: traceid checks ignoram layout roadmap_namespacing: by_agent"
status: wip
req: docs/requisições/claude/REQ-2026-06-14-traceid-by-agent-support.md
created: 2026-06-14
author: zeus
branch: fix/v2.5.2-traceid-by-agent
---

# Roadmap v2.5.2 — Fix traceid checks em by_agent

> Criado em: 2026-06-14 | Status: 🔄 WIP

## Diagnóstico

Em projetos com `roadmap_namespacing: by_agent`, os 5 checks `traceid_*` silenciam
completamente — exit 0 com zero violações mesmo com pares `req_id` quebrados.

**Causa raiz por CLI:**

| CLI | Função | Problema |
|-----|--------|---------|
| Go | `collectTraceIdEntries` | varre `rootDir/<estado>/` apenas; by_agent tem `rootDir/<agente>/<estado>/` |
| Python | `_index_roadmaps` | mesmo problema — itera explicitamente sobre estados canônicos sem subir um nível |
| Node.js | `walkMd` + `stateFromPath` | **já funciona** — `walkMd` é recursivo e `stateFromPath` deriva estado do path |

**Impacto:** CMDB bloqueado de migrar R9/R10 do gate interno para o trackfw (ADR-039 §4).

**Salvaguarda necessária em todos os CLIs:** quando `trace_id_field` está setado mas
nenhuma REQ nem Roadmap é indexado, emitir warning de configuração em vez de silenciar.

---

## Wave 1 — Fix by_agent + salvaguarda (3 MLs em paralelo)

> Dependências: independente entre CLIs

### ML-1A — Go

**Status:** ⬜ Pendente

**Arquivo afetado:** `internal/validator/validator_traceid.go`

**Contexto:**
- `validateTraceId(cfg config.ProjectConfig)` já recebe o config completo (linha 88)
- `collectTraceIdEntries(rootDir, traceField string)` — função interna, só flat (linha 22)
- `cfg.RoadmapNamespacing` tem tipo `config.NamespacingByAgent` quando `by_agent`
- `cfg.Agents` é `[]string` com a lista de agentes (pode ser vazia → descoberta automática via `os.ReadDir`)
- Padrão a seguir: `resolveWIPDirs` em `validator.go` (linha 706–727) — lógica idêntica

**Ações:**

1. Em `validator_traceid.go`, adicionar função interna `collectTraceIdEntriesByAgent`:

```go
func collectTraceIdEntriesByAgent(roadmapDir, traceField string, cfg config.ProjectConfig) ([]traceIdEntry, error) {
    stateDirs := []string{"wip", "done", "backlog", "blocked", "abandoned"}
    agents := cfg.Agents
    if len(agents) == 0 {
        entries, err := os.ReadDir(roadmapDir)
        if err == nil {
            for _, e := range entries {
                if e.IsDir() {
                    agents = append(agents, e.Name())
                }
            }
        }
    }
    var all []traceIdEntry
    for _, agent := range agents {
        agentDir := filepath.Join(roadmapDir, agent)
        for _, state := range stateDirs {
            dir := filepath.Join(agentDir, state)
            files, err := filepath.Glob(filepath.Join(dir, "*.md"))
            if err != nil || files == nil {
                continue
            }
            for _, f := range files {
                content, err := os.ReadFile(f)
                if err != nil {
                    continue
                }
                val := extractFrontmatterField(string(content), traceField)
                if val == "" {
                    continue
                }
                all = append(all, traceIdEntry{reqID: val, state: state, path: f})
            }
        }
    }
    return all, nil
}
```

2. Em `validateTraceId`, substituir a linha de indexação de roadmaps (linha 97):

```go
// ANTES:
roadmapEntries, _ := collectTraceIdEntries(cfg.RoadmapDir, traceField)

// DEPOIS:
var roadmapEntries []traceIdEntry
if cfg.RoadmapNamespacing == config.NamespacingByAgent {
    roadmapEntries, _ = collectTraceIdEntriesByAgent(cfg.RoadmapDir, traceField, cfg)
} else {
    roadmapEntries, _ = collectTraceIdEntries(cfg.RoadmapDir, traceField)
}
```

3. Após montar os índices (após linhas 100–107), adicionar salvaguarda de zero entradas:

```go
if len(reqEntries) == 0 && len(roadmapEntries) == 0 {
    warnings = append(warnings,
        "trace_id_field is set but no REQ/Roadmap entries were indexed — check req_dir, roadmap_dir and roadmap_namespacing")
    return violations, warnings
}
```

4. Atualizar `internal/validator/validator_traceid_test.go`:
   - Adicionar fixture `testdata/traceid-by-agent/` com estrutura:
     ```
     req/REQ-001.md            (req_id: orphan-001)
     roadmaps/claude/wip/rm.md (req_id: orphan-002)
     ```
   - Adicionar teste `TestValidateTraceId_ByAgent` que configura `RoadmapNamespacing: config.NamespacingByAgent` e verifica que `traceid_orphan_req` e `traceid_orphan_roadmap` disparam.
   - Adicionar teste `TestValidateTraceId_ZeroEntriesWarning` para a salvaguarda.

**Critérios de aceite:**
- [ ] `go test ./internal/validator/ -run TestValidateTraceId -v` verde com casos by_agent
- [ ] `go test ./internal/... -q` sem regressões (ignorar `TestMoveRoadmap_ByAgent` pré-existente)

**Comandos de validação:**
```bash
cd /Users/kgsaran/Sistemas/Desenvolvimento/workspace/trackfw
make build
go test ./internal/validator/ -run TestValidateTraceId -v
go test ./internal/... -q 2>&1 | grep -v "TestMoveRoadmap_ByAgent"
```

---

### ML-1B — Node.js

**Status:** ⬜ Pendente

**Arquivo afetado:** `npm/src/validator/traceid.js`, `npm/tests/traceid.test.js` (criar se não existir) ou arquivo de teste relevante

**Contexto:**
- `walkMd(dir)` (linha 39) já é recursivo → encontra arquivos em `roadmapDir/<agente>/<estado>/` ✅
- `stateFromPath(filePath)` (linha 30) percorre segmentos de trás para frente → retorna estado correto ✅
- O Node.js **já funciona para by_agent na lógica de indexação**
- Falta: (a) teste de regressão explícito para by_agent; (b) salvaguarda de zero entradas

**Ações:**

1. Em `checkTraceIds` (linha 62), após construir `reqIndex` e `roadmapIndex`, adicionar salvaguarda:

```js
// Salvaguarda: nenhuma entrada indexada → warning de configuração
if (reqIndex.size === 0 && roadmapIndex.size === 0) {
  return [
    '__warning__:trace_id_field is set but no REQ/Roadmap entries were indexed — check reqDir, roadmapDir and roadmap_namespacing'
  ]
}
```

> **Nota sobre formato do warning:** verificar como o caller em `index.js` (linha 799) distingue
> violations de warnings. Se não houver mecanismo de distinção, usar prefixo `__warning__:` ou
> retornar objeto `{violations: [], warnings: [msg]}`. Ajustar conforme o padrão do projeto.
>
> **Alternativa mais simples:** retornar a mensagem como violation com prefixo de regra
> `traceid_config_warning`, que o caller mapeia para warning via `applyRule`.
> Consultar como outros warnings são gerados em `index.js` e mimetizar.

2. Criar `npm/tests/traceid_by_agent.test.js` (ou adicionar ao teste existente) com fixture
   in-memory (criar dirs temporários em `/tmp`) que simula estrutura by_agent:
   ```
   /tmp/traceid-by-agent-test/
   ├── req/REQ-001.md            (req_id: orphan-001)
   └── roadmaps/claude/wip/rm.md (req_id: orphan-002)
   ```
   Verificar que `checkTraceIds(reqDir, roadmapDir, 'req_id')` retorna `traceid_orphan_req` e
   `traceid_orphan_roadmap`.

**Critérios de aceite:**
- [ ] Teste by_agent verde: violations corretas com layout `roadmapDir/<agente>/<estado>/`
- [ ] Salvaguarda: zero entradas → warning de configuração emitido

**Comandos de validação:**
```bash
node npm/tests/traceid_by_agent.test.js   # ou nome do arquivo criado
node npm/tests/validate_json.test.js
node npm/tests/help.test.js
```

---

### ML-1C — Python

**Status:** ⬜ Pendente

**Arquivo afetado:** `pypi/trackfw/traceid.py`, `pypi/tests/test_traceid.py` (criar se não existir) ou arquivo existente

**Contexto:**
- `_index_roadmaps(roadmap_dir, field)` (linha 90) itera sobre `_ROADMAP_STATES` e varre
  `roadmap_dir/<state>/` — não encontra nada em layout `roadmap_dir/<agente>/<estado>/`
- `check_traceid(cfg)` recebe o dict de config inteiro (linha 127) — tem `cfg.get("roadmap_namespacing")` e `cfg.get("agents", [])`

**Ações:**

1. Em `traceid.py`, adicionar função `_index_roadmaps_by_agent`:

```python
def _index_roadmaps_by_agent(roadmap_dir: str, field: str, agents: list) -> list:
    """Indexa roadmaps em layout by_agent: roadmap_dir/<agente>/<estado>/"""
    if not agents:
        try:
            agents = [e for e in os.listdir(roadmap_dir)
                      if os.path.isdir(os.path.join(roadmap_dir, e))]
        except OSError:
            return []
    entries = []
    for agent in agents:
        agent_dir = os.path.join(roadmap_dir, agent)
        for state in _ROADMAP_STATES:
            state_dir = os.path.join(agent_dir, state)
            try:
                names = [n for n in os.listdir(state_dir)
                         if n.endswith(".md") and not os.path.isdir(os.path.join(state_dir, n))]
            except OSError:
                continue
            for name in names:
                path = os.path.join(state_dir, name)
                try:
                    with open(path, "r", encoding="utf-8") as f:
                        content = f.read()
                except OSError:
                    continue
                trace_id = _extract_trace_id(content, field)
                if trace_id:
                    entries.append({"file": name, "path": path, "trace_id": trace_id, "state": state})
    return entries
```

2. Em `check_traceid(cfg)`, substituir a chamada de indexação de roadmaps (linha 140):

```python
# ANTES:
roadmap_entries = _index_roadmaps(roadmap_dir, field)

# DEPOIS:
namespacing = cfg.get("roadmap_namespacing", "")
if namespacing == "by_agent":
    agents = cfg.get("agents", [])
    roadmap_entries = _index_roadmaps_by_agent(roadmap_dir, field, agents)
else:
    roadmap_entries = _index_roadmaps(roadmap_dir, field)
```

3. Após montar `req_entries` e `roadmap_entries`, adicionar salvaguarda:

```python
if not req_entries and not roadmap_entries:
    return [_violation(
        "traceid_config_warning",
        "",
        "trace_id_field is set but no REQ/Roadmap entries were indexed"
        " — check req_dir, roadmap_dir and roadmap_namespacing"
    )]
```

4. Criar/atualizar `pypi/tests/test_traceid.py` com fixture em `tmp_path` (pytest):
   - Estrutura by_agent:
     ```
     req/REQ-001.md            (frontmatter: req_id: orphan-001)
     roadmaps/claude/wip/rm.md (frontmatter: req_id: orphan-002)
     ```
   - Chamar `check_traceid({"req_dir": req_dir, "roadmap_dir": roadmap_dir, "trace_id_field": "req_id", "roadmap_namespacing": "by_agent"})`
   - Verificar regras `traceid_orphan_req` e `traceid_orphan_roadmap` nos resultados.
   - Adicionar teste para salvaguarda de zero entradas.

**Critérios de aceite:**
- [ ] `pytest pypi/tests/test_traceid.py -v` verde com casos by_agent
- [ ] `pytest pypi/tests/ -q` sem regressões

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_traceid.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Legenda
- ⬜ Pendente
- 🔄 Em andamento
- ✅ Concluído
- ❌ Bloqueado
